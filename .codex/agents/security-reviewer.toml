name = "security-reviewer"
description = "Read-only privacy, authorization, secret, and mutation reviewer."
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
approval_policy = "never"
developer_instructions = "Read-only. Review privacy, authorization, secrets, protected tests, and mutation evidence. Ponytail is lite."
