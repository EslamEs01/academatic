# Contract: Targeted Visual Grounding

**Purpose**: every teacher internal page traces to a captured legacy teacher capability — no invented SaaS, no pixel clone.

## Binding rules

- Each of the 7 pages MUST cite its T-capability + evidence path (`teacher-legacy-coverage.md`): schedule=T14, students=T8, outcomes=T22/T3, tasks=T11/T16, reports=T9+T20/T21, profile=T23, library=T15.
- Legacy = capability coverage, NOT a pixel/grid clone (agenda cards, not the 7×24 grid; cards, not tables).
- Where legacy evidence is thin (live-room T7, availability-edit, certificate-submit), the capability is an honest backendRequired gate + the gap is recorded.
- Excluded-by-law pay surfaces (T2/T17/T18/T19) are NEVER rendered.

## Acceptance

- `teacher-legacy-coverage.md` accounts for every T1–T27 with a 025 disposition; each built page maps to its T#; no page is ungrounded.

**Stop**: any page without a captured capability or an honest-gate justification → STOP.
