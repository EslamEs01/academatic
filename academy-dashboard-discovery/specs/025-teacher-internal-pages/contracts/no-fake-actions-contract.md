# Contract: No Fake Actions

**Purpose**: every unavailable action across the 7 pages is a labeled honest gate — the four-class honesty law.

## Rules
- Unavailable actions render as `gateNote` / `guidePanel` / `plannedCard` (backendRequired or planned), labeled with a reason.
- Real links point ONLY to real pages; planned items are non-anchor buttons («قريبًا»).
- Zero `href="#"`; zero dead buttons; zero unexplained disabled controls; zero fake dynamic state.

## Action inventory (all backendRequired gates)
schedule: enter/live-room, availability-edit · outcomes: save/submit · tasks: complete/assign · reports: export/download · profile: photo/save/password · library: upload/download · students: contact (if surfaced).

## Acceptance
- The smoke dead-nav/dead-button/disabled-control sweep passes for all 7 new pages; zero `href="#"`; every gate labeled + aria-safe.

**Stop**: any fake save/submit/upload/download/join/complete, or a dead button → STOP.
