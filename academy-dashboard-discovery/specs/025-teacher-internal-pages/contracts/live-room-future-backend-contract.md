# Contract: Live-room Future-Backend (T7 / Spec 024 B-04)

**Purpose**: teacher live/room actions are honest gates only — no real capture exists.

## Rules
- Any enter/join/start/room affordance (teacher-schedule, teacher-outcomes) = a labeled backendRequired `gateNote` — never a working control.
- Recorded future-backend (carried from Spec 024 B-04).

## Forbidden
- Fake enter/start/end-class; fake attendance write; fake live-room; meeting/Zoom engine; camera/mic controls; `href="#"`.

## Acceptance
- Every live/room affordance is a backendRequired gate; no fake join anywhere in the teacher app; smoke confirms the gate is honest (labeled, non-activating).
