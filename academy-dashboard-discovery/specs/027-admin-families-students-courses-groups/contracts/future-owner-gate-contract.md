# Contract — Future-Owner Gate

**MUST**: every out-of-scope action stays an honest gate + is recorded against an owner in `future-owner-register.md`; NOT built in 027.

**Acceptance**
- M-N assign-teacher persistence → **028**; M-O message → **026/future-backend**; M-P print/export → **029**; M-Q billing/plan → **030**; M-T feedback/reports → **029**; M-U login-as/reset → **future-backend**; M-V materials/subjects → **031**.
- Each stays an honest `data-disabled-reason`/`data-coming-soon` gate; smoke asserts non-dead.
- **Fail** if an out-of-scope domain is built in 027 or a gate goes dead.
