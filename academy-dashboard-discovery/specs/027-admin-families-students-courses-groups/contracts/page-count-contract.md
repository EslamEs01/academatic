# Contract — Page Count

**MUST**: current = 97; target after 027 = **97** (zero new pages). Every delta is a modal/drawer/picker/kebab/tab/gate on an existing page. A new standalone page is allowed ONLY if planning proves a modal/drawer is insufficient AND legacy IA requires it — none does.

**Acceptance**
- `find public -maxdepth 1 -name '*.html' | wc -l` = 97 after build.
- Smoke count assert stays 97.
- The 4 planned nav items (studentResult/studentEvaluation/scheduleSearch/familyCategories) remain planned gates (content via existing tabs/modals).
- **STOP** if a new page appears or count ≠ 97.
