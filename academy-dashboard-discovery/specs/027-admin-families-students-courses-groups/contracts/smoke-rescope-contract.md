# Contract — Smoke Rescope

**MUST**: extend `tests/smoke/run.cjs` additively. Keep `payHit`/`famPay`/`payFigure`/child-view/admin-finance + the Spec-026 action-completion asserts **byte-verbatim**.

**Acceptance (new asserts)**
- Count stays **97**; the 9 pages load.
- `students.html` has a working row kebab (View real · Edit modal · Suspend/Remove confirm), each honest.
- Enroll/assign/move open a modal/drawer/gate; edit family/student/course/group open modals (not bare toasts).
- Results/Evaluation carry **no computed score/rank/chart**.
- `href="#"`=0; no dead buttons; no fake final (create/save/delete/enroll/assign/remove); no «preview action».
- **One sanctioned amendment**; protected asserts unchanged.
- **Fail** if a protected assert is weakened or a new action is unasserted.
