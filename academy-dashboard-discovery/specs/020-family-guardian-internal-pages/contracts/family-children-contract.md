# Contract — family-children (Spec 020)
Bands: pageHead · summary band (children count + quota-context line) · FIVE child cards over the REAL roster st1/st6/st11/st12/st13 — each: avatar/name/level · course/group/teacher labels · lifecycle chip (familyStatusChip) + subscription label · authored progress bar + attendance hint · latest-note/next line (kidHints/nextOrNoteKey) · the REAL drill-down `family-child(.en).html#child=stX` — · truthful empty pattern · note. HARD PINS: body anchors === 5 (each child exactly once); zero forms; zero dead links. Grounding: the "All Account Subscriptions" frames (every legacy column resolves to a card element or the drill-down).
