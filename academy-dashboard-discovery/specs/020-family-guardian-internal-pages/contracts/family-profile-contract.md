# Contract — family-profile (Spec 020)
Bands: pageHead · guardian identity card (avatar/name/role chip) · family/account rows (email · city · joined · children count — the RETAINED 014 acct register re-homed) · children summary line (5 names/count, display-only) · preference chips (language/theme/contact) · EXACTLY THREE backendRequired gates as plannedCards: photo upload · profile save · password change (the legacy profile-edit write surface, frame-proven) · note. HARD PINS (smoke): zero `<form>/<input>/<select>/<textarea>`; plannedBackend === 3 on this page; body anchors === 0. Grounding: profile-edit frames (+ the 500 view page — display-only is the fix).
