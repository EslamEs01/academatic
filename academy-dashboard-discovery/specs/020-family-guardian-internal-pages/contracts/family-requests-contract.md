# Contract — family-requests (Spec 020)
Bands: pageHead · summary band (request counts by status — authored) · type-grouped request cards re-homing the RETAINED prt.fam.req.* register: TRIAL (new/existing two-path tiles; step-2 stays gated — the recorded gap, never invented) · MEETING (truthful empty + the meetingRequest gate) · FEEDBACK (the four question lines preview) · CANCEL/RESCHEDULE (the two options + the caution note) — each closed by a labeled backendRequired gate · note. HARD PINS: body anchors === 0; zero forms; zero fake submit/send/cancel; every status chip labeled icon+text. Grounding: the feedbacks (meetings table) + request-trial wizard frames + the admin feedback-students table shape.
