# Contract — family-billing / ZERO-PAY (Spec 020)
Bands: pageHead · hour-quota tiles (authored 40/12/28 — the legacy Total/Remaining/Taken idea; hours are session units, never money) · settled-status chip + reassurance (the RETAINED 014 billSettled/billReassure re-homed) · per-child subscription chips · invoice STATUS rows `{serial label · month label · due label · course · labeled status chip}` — **the fixture shape has NO amount field; the legacy Amount column is deliberately dropped** · backendRequired finance gates + the admin-finance-needed note · note. HARD PINS: body anchors === 0; zero payment/receipt/pay controls of any kind; zero forms; the VERBATIM zero-pay payFigure regex passes on this body (and on all 7 family internals); billing copy pre-checked against the regex BEFORE build (D13 wording rules). Grounding: the billing frames (full column set) + the home hour-quota banner.
