# Contract — family-child Preservation (Spec 020)
`family-child(.en).html` REMAINS the drill-down: NOT a sidebar item (its PAGES entry/activeId untouched → home stays its active anchor; navCurrent===2×family-portal BYTE-KEPT). `family-child.js` is NOT touched (outside the allowed list by default); its body inputs (CHILD_PROFILE/CHILD_ORDER/FAMILY_PREVIEW/students/sessions/prt.child.*) are untouched → BODY BYTE-EQUAL BY CONSTRUCTION, PROVEN at the G-audit via #page-body extraction hash pre/post. Its nav REBAKES (8 links, multiset 19) — the ONLY sanctioned change to the pair. All smoke body asserts for it (5 panels · default st1 · 5 switcher links · zero-pay) BYTE-KEPT. Deep links from home (5), children (5), progress (5) all target it. STOP-CONDITION: any body drift halts implementation for a report.
