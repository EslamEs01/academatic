# Contract — Honesty / backendRequired (Spec 020)
Four honest action classes only. ZERO fake: payment · payment link · receipt upload · invoice pay · request submit · feedback send · cancel/reschedule submit · material download · profile save · password update · live join · chat · notifications · attendance write. Every unavailable action = a labeled availability chip (plannedCard/gateNote) — never an anchor, never color-only. Zero `href="#"`; zero dead links; zero dead filters (child grouping replaces filter theater). The home quick-tiles MUST tell the truth post-flip (implemented → real links — the proven 019 honesty law).
