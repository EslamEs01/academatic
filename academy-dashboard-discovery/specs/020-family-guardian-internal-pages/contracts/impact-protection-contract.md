# Contract — Impact Protection (Spec 020)
PROTECTED BYTE-IDENTICAL (59/77): 40 admin files · index.html · portals hub pair · ALL 14 student files · teacher home pair. CHANGED (18): 14 NEW family internals + `family-portal(.en).html` (nav + status-aware quick-tiles band ONLY; the 5 child cards and every other band byte-equal) + `family-child(.en).html` (nav-only; body byte-equal, extraction-proven). `public/assets/*` regeneration = reported churn. SOURCE scope per plan.md §Allowed; `portal-shell.js`/`enhance.js`/`nav.config.js`/`package.json`/all student/teacher/admin modules FROZEN (0-line diffs proven). If any count differs → report honestly with the file list BEFORE proceeding.
