# Contract — Family Page Shell (Spec 020)
All seven pages render inside Shell v2 via additive PAGES entries `{ shell:'portal', role:'family', personaKey:'data.fam.fam1.name', activeId:'<navId>', titleKey:'prt.title.fam<X>' }` — 7 imports + 7 entries, ZERO engine change (the 019 pass-through is live). `portal-shell.js` UNTOUCHED. Per page: its nav item `is-active`+`aria-current` ×2 instances; home a plain link; shell multiset 19, unique {family-portal + 7 internals + hub}. Body anchors 0 except the pinned drill-down sets (children 5 · progress 5). Violet accent via `data-role="family"` tokens; `portal-page.js` primitives; admin chrome never imported.
