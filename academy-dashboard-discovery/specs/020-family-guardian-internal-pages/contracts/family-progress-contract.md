# Contract — family-progress (Spec 020)
Bands: pageHead · family summary band (attendance trio re-rendered from the RETAINED 014 register) · FIVE per-child progress cards (authored `.pt-bar` + attendance mini + signal/hint line + the REAL `#child=stX` drill-down link) · teacher-notes band (the RETAINED 014 teacherNotes re-homed) · truthful empty · note. HARD PINS: body anchors === 5 exact; zero charts/rank/percentile/peer-comparison; bars = `.pt-bar` only. Grounding: the Classes-Summary select+submit frames (per-child inspection kept, form dropped) + the no-legacy-progress-page gap (authored figures only).
