# Research & Decisions — Spec 020 (D1–D20)

Grounding: [`visual-grounding.md`](visual-grounding.md) (**27/27 family frames**, inventories greped, 5 gaps recorded) · Specs 016–019 law · direct source reads this cycle: `portal-shell.js` (untouched law), `portal-page.js` (the shipped shared primitives), `build-html.mjs` (the 019 `activeId: p.activeId || 'home'` pass-through is LIVE), `fixtures/portal.js` (ROLE_NAV + retained 014/018 slices + CHILD_PROFILE/CHILD_ORDER + the 019 STUDENT_PAGES precedent), `family-portal.js` (its own quickTiles copy + the 5 child links), `family-child.js` (pure-CSS `:target`; consumes CHILD_PROFILE/CHILD_ORDER/FAMILY_PREVIEW — none touched by 020), the smoke portal block (student branch + family/teacher/hub/family-child branches), a11y/capture matrices.

**D1 — Grounding completeness.** COMPLETE at 100% — all 27/27 family frames personally viewed (16 opened at specify time incl. every interaction shot); the 9 required areas each carry an evidence row; 5 gaps recorded with nearest-evidence resolutions (trial step-2 gated not invented · no legacy progress page · profile view 500 · no cancel/certificate frames · Amount column deliberately excluded). **No amendment needed.**

**D2 — Module architecture.** Seven independent page modules `src/js/pages/family-{children,schedule,progress,billing,requests,materials,profile}.js`, each exporting `renderFamily<X>()` returning baked band HTML (the 019 pattern verbatim): `pageHead` → summary band → 1–3 short content bands → labeled gates → closing `.pt-note`. ≤2 screens each.

**D3 — Shared primitives.** REUSE `components/portal-page.js` as-is (pageHead/secHead/kpiCard/kpiRow/plannedCard/gateNote) for all seven modules — zero edits to it expected; if a primitive gap appears it is EXTENDED additively (recorded), never forked. The 018 home modules keep their own copies (identity law).

**D4 — Build registration.** `build-html.mjs`: **7 imports + 7 PAGES entries ONLY** (the 019 pass-through already ships per-page `activeId`): `{ base: 'family-<x>', shell: 'portal', role: 'family', personaKey: 'data.fam.fam1.name', activeId: '<navId>', titleKey: 'prt.title.fam<X>', render }`. NO engine-line change this time — the touch is purely additive 14 lines.

**D5 — Nav flip.** Exactly seven one-line edits in `ROLE_NAV.family`: `status: 'planned' → 'implemented'` (children/schedule/progress/billing/requests/materials/profile). Order/labels/icons/pages FROZEN. `ROLE_NAV.student` (7×implemented) and `ROLE_NAV.teacher` (1+6) byte-untouched.

**D6 — FAMILY_PAGES fixture group** (additive, `fixtures/portal.js`):
- `quota`: `{ totalH: 40, takenH: 12, remainH: 28 }` — authored hour counts, internally consistent (40 = 12+28; takenH mirrors the 014 attended=12 register; hours are figure-safe session units, never money).
- `children`: per-child snapshot rows re-referencing existing canon `{ childId, subscriptionKey (from the fam1 plan register), nextOrNoteKey }` — identity/course/teacher/progress/status resolve from students.js + SUBJ maps + kidHints (RETAINED).
- `billing`: `{ invoices: [3 authored rows { id, serialKey, monthKey, dueKey, courseId, statusTone: 'completed'|'upcoming' }] }` — NO amount field exists in the shape (zero-pay by construction).
- `requests`: `{ items: [4 type cards { type: trial|meeting|feedback|cancel, statusTone, previewKeys (the RETAINED prt.fam.req.* re-referenced) }] }`.
- `materials`: reuse `FAMILY_PREVIEW.materials` (st1/st11/st6) + 2 NEW authored items (st12, st13) so all five children have ≥1 item.
- `profile`: `{ prefs: [3 chips], gates: ['photoUpload','profileSave','passwordChange'] }` (the guardian profile-edit write surface — same trio the frames prove).
Schedule needs NO new slice (SESSIONS_FULL + todayChildren + SCHEDULE_WEEK proxy). Progress reuses students.js progress + kidHints + teacherNotes + attendance (all RETAINED). Rules: additive only; zero deletion/rewording; everything consistent with fam1 + the five children + the family-child panels.

**D7 — Locale namespace.** Additive, mirrored: `prt.title.fam{Children,Schedule,Progress,Billing,Requests,Materials,Profile}` + `prt.fam.pg.{kids,sched,prog,bill,req,mat,prof}.*` (headers/hints/sections/gates/chips/empties) + `data.prtFamPg*` authored strings. Existing `prt.fam.*` keys REUSED verbatim wherever the copy fits (todayTitle/todayFor/att.*/billSettled/billReassure/req.*/notesTitle/kidHints…), never reworded. Billing keys authored AGAINST the zero-pay regex before build (D13).

**D8 — activeId per page.** Each entry passes its nav id (children/schedule/progress/billing/requests/materials/profile) through the LIVE pass-through; shell untouched; home a plain link on internals.

**D9 — family-child activeId.** UNCHANGED: its PAGES entry keeps `activeId: null` → shell default `'home'` → after the flip its nav renders 8 LINKS with home active (`navCurrent === 2× family-portal` — the existing assert byte-KEPT). Correct drill-down semantics preserved; NOT a sidebar item.

**D10 — Anchor inventories (smoke-pinned).** Every family-shell page (home + 7 internals + family-child): shell = 8 nav ×2 instances + hub×3 = **multiset 19**; unique set = {family-portal + the 7 internal pages + portals} = 9 targets. Body: home **12** (D11) · children **5** (exact `family-child(.en).html#child=stX`, each once) · progress **5** (same set) · schedule/billing/requests/materials/profile **0** · family-child **5** hash-switcher links (BYTE-KEPT).

**D11 — Home quick-tiles honesty fix.** `family-portal.js`'s OWN `quickTiles` copy becomes status-aware (the exact 019 student edit): implemented → real `<a class="pt-qtile">` language-correct link; planned → the existing tile pattern (none remain after the flip). Home body anchors 5→**12** = the 5 child drill-downs (UNCHANGED band) + 7 sibling tiles; smoke pins BOTH subsets exactly (child-link regex set === {st1,st6,st11,st12,st13}; tile regex set === the 7 sibling pages). All other home bands byte-equal.

**D12 — family-child nav rebake + body byte-equality.** `family-child.js` is NOT in the allowed list (default holds); its body inputs (CHILD_PROFILE/CHILD_ORDER/FAMILY_PREVIEW/students/sessions + `prt.child.*` keys) are all untouched by 020 → body byte-equal BY CONSTRUCTION. PROOF at G-audit: extract the `#page-body` region from built `family-child(.en).html` pre/post (the 017 extraction technique) and byte-compare; plus the smoke branch's body asserts (5 panels · default st1 · 5 switcher links · zero-pay) run BYTE-KEPT. If extraction differs → STOP and report before proceeding (the spec's stop-condition).

**D13 — Billing zero-pay wording strategy.** Authoring rules (checked BEFORE build against the exact smoke regex `/ريال|ر\.س|\bSAR\b|\bUSD\b|جنيه|\bEGP\b|[$€£]|ادفع|سداد|pay now|payment|\bamount\b|\bprice\b|مبلغ|سعر|رسوم/i`): hour units only («ساعة/ساعات», counts via `num()`); invoice rows = serial label (e.g. «ف-٢٠٢٦-٠٦-٠١») + month label + due label (date words only) + course name + labeled status chip (مسوّاة/قادمة → completed/upcoming tones); gates worded «تفاصيل الفواتير وإتمامها تُدار من إدارة الأكاديمية وتتطلب الخادم» (zero pay-action verbs). **Regex extension plan**: the family-internal smoke branch RUNS THE IDENTICAL payFigure regex body on every one of the 7 new pages (a verbatim REUSE — the 018 family-child precedent — never a mutation of the original lines, which stay byte-verbatim).

**D14 — Smoke amendment scope.** ONE amendment: (a) PAGES/PORTAL_PAGES +7 → 38 bases / **76 loads**; (b) NEW `FAMILY_INTERNAL` set + branch: role=family · active ===2×self · navListAnchors===8 · plannedNavAnchors===0 · shell multiset===19, unique === the 9-target set · formControls===0 · tables===0 · ceiling [500,2200] · the verbatim payFigure regex on every page · per-page pins (children bodyAnchors===5 exact + ≥5 cards; progress ===5 exact + bars ≥5; schedule bodyAnchors===0 + ≥1 gate-note; billing ===0 + zero-pay + plannedBackend===1; requests ===0 + plannedPlanned+plannedBackend per the gate table; materials ===0 + ≥5 items; profile ===0 + plannedBackend===3 + 0 forms); (c) family HOME re-scope: bodyAnchors 5→===12 (two exact subsets), navListAnchors 1→8, multiset 5→19; planned cards stay 1+1; (d) family-child re-scope: navListAnchors 1→8, multiset 5→19, unique→the 9-target set; navCurrent 2×family-portal KEPT; ALL body asserts BYTE-KEPT; (e) `expPlanned` +7 entries (children 0 · schedule 0 · progress 0 · billing 1 · requests 1 · materials 1 · profile 3 — re-pinned at implementation if the gate placement shifts, enumerated in the diff); (f) **BYTE-VERBATIM: the payHit block · the original zero-pay regex LINES (home + family-child) · the ENTIRE student branch (home + internals) · the teacher branch · the hub branch · ALL admin asserts**. The Shell-v2 `else` branch shrinks to teacher-portal only (family-portal moves into the new family branch) — the teacher values (7/5/1) byte-kept.

**D15 — Identity target.** 77 built. Changed: 14 NEW + `family-portal(.en).html` (nav + tiles band) + `family-child(.en).html` (nav-only) = 18 → **59/77 hash-identical** (40 admin + index + hub pair + 14 student files + teacher pair). Asset regeneration reported as churn.

**D16 — Student stability.** No student file in the allowed list; the smoke student branch byte-verbatim (its asserts re-run green on all 16 student loads); the hash audit lists all 14 student files among the 59 identical; student nav stays 7 implemented by not touching the registry.

**D17 — Teacher safety.** Registry rows byte-untouched (grep-proof); teacher module 0-diff; payHit byte-verbatim; the extended-set three-layer grep re-run at the G-audit (tripwire, not new-content review).

**D18 — Ceiling.** YES — [500, 2200] @1366×768 on all seven internals (the 019 precedent); homes keep [900, 2200] byte-verbatim; recorded heights per page in REVIEW.md.

**D19 — Capture matrix.** Additive rows: 7 pages × (AR light desktop + AR light mobile) = 14 · billing AR dark (the token-densest) · children EN desktop · family home refresh (`tiles-now-links` variant) · family-child refresh proof (default st1) · unchanged proofs (student home + teacher home + admin dashboard re-captured byte-stable). Full-page; zero console errors.

**D20 — Coverage annotations.** Append-only: (a) 016 matrix gains the Spec-020 delivery annotation (§8 F-rows → delivered/gated per page, guardian-proxy citations); (b) 018's displacement map family rows → "re-homed by 020" note (children cards→children page · today→schedule · signals/notes/history→progress · subs/billing→billing · requests hub→requests · materials→materials · account→profile; the RETAINED keys now render again); (c) 019 needs NO annotation (student untouched) — cited only; (d) CLAUDE.md SPECKIT block + README (the Django note: "family internal pages use the same ROLE_NAV registry and active_id; planned entries flip one line at a time; family-child remains a drill-down page linked from family contexts").
