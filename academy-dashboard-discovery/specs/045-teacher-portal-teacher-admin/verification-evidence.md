# Verification Evidence

**STATUS: FINAL.** This file records **one canonical closure state**. §1–§7 are current. Everything
under "HISTORICAL / SUPERSEDED" at the bottom is retained provenance and is **not** a status claim.

**Implementation-run baseline HEAD:** `32e51e5` — the Spec-045 artifact-only commit. Its **app bytes
are identical to `722be1c`** (the accepted, independently verified Spec-044 tree); `32e51e5` touched
only `specs/045-…/`, `.specify/feature.json` and `AGENTS.md`. This is the baseline every impact
number is measured **against**.

**Current pushed branch HEAD:** `7e30474` — contains the Spec-045 implementation. This is the tree
every gate below was **run on**. `git rev-parse HEAD` = `git rev-parse origin/045-teacher-portal-teacher-admin`
= `7e3047450719ce366753d0df306a407742240bc3`.

These two are **not** the same commit and are never used interchangeably: measuring impact against
`7e30474` would return 0 changed bodies, because that commit *is* the implementation.

Pre-implementation evidence is in `baseline-ledger.md`.

---

## 1. Final gate results — all re-run on the pushed tree, 2026-08-04

Every row is live command output from this reconciliation run. There is no "partial" column any
more: each gate was run to completion on the final bytes.

| Gate | Command | Result | Exit |
|---|---|---|---|
| Build | `npm run build` | **PASS** — 114 static pages + `index.html` + `.nojekyll` = **115 HTML** | 0 |
| Rebuild determinism | `git status --porcelain` before **and** after `npm run build` | **PASS** — **0 lines both times**: the committed generated bytes reproduce exactly from the committed source | — |
| Source/generated parity | `parity.cjs` (snapshot → canonical regenerate → byte compare) | **PASS** — all **22** localized Teacher consumers reproduce **byte-identically**; refuses to pass unless it snapshotted exactly 22 | 0 |
| Domain audit | `sp045-audit.cjs`, 9 fail-loud sections | **PASS — 9/9** (breakdown in §2) | 0 |
| Smoke | `npm run test:smoke` | **PASS — 114 page loads**, no raw keys / external requests / dead buttons / unexplained disabled controls | 0 |
| Smoke (re-run after the test correction in §6) | `npm run test:smoke` | **PASS — 114 page loads** | 0 |
| Accessibility | `npm run test:a11y` | **PASS — 300 scenarios, all clean, `critical=0 serious=0`** (51 of the 300 are Teacher-scope) | 0 |
| Spec-044 interaction | `node tests/interaction/run.cjs` | **PASS — 22/22, 0 RED** | 0 |
| Project screenshots | `npm run screenshots` | **PASS — 436 captured · 0 with console errors** | 0 |
| Teacher screenshot matrix | `final-matrix.cjs` | **PASS — 64 frames · 0 console errors · 0 overflow · 0 undersized · 9/9 state assertions** | 0 |
| Rendered inspection | `inspect.cjs` | **PASS — 19 measured visual claims, 19 verified, 0 failed** | 0 |
| Locale parity | flattened key-set comparison, both directions | **PASS — `prt` 670/670, `trn` 220/220, 0 divergence either way** | — |
| Impact vs `32e51e5` | `impact.cjs 32e51e5` (strict `#page-body` vs committed bytes) | **PASS** — 115 HTML · **0 added / 0 removed** · **26 bodies changed** (Teacher 22, non-Teacher 4) · 88 unchanged · declared collateral **4/4** · **UNDECLARED drift 0** · parse errors 0 | 0 |
| Impact vs `722be1c` | `impact.cjs 722be1c` | **PASS — identical numbers**, confirming `32e51e5` is app-byte-equivalent to `722be1c` | 0 |
| Whitespace | `git diff --check -- academy-dashboard-discovery/app` | **PASS — clean** | 0 |
| Mutations | 17 isolated runs | **17/17 proven causal RED then GREEN** (M45-01…M45-16 + M45-17) | — |
| Mutation residue | `ls -d /tmp/sp045-mut-*` | **0** — returns nothing | — |

### Two infrastructure REJECTS, recorded not counted

The first attempt at smoke, a11y and screenshots in this run all exited non-zero on
`EADDRINUSE :::4178` — an **orphaned prior-session `tests/a11y/run.cjs`** still held the fixed smoke
port. That is the same environment trap the mutation campaign recorded twice before (M45-09's port
collision, the OOM-killed renderer). It is **not** a gate failure and is not counted as one: the
orphan was identified by PID, cleared, the port verified free, and all three gates re-run to the
green results in the table above. Recorded because an exit code that is right for the wrong reason is
the exact failure mode this project has twice been bitten by.

## 2. Domain audit breakdown (9/9 PASS)

1. **11 scopes / 22 localized consumers** present, each with a `#page-body`.
2. **0** user-visible pay tokens across the 22 consumers.
3. **0** non-negated score/rank/leaderboard/percentile tokens; **0** chart/`<canvas>`.
4. **0** `teacher-performance` references in any of the 16 portal consumers.
5. `teacherAbsent`/`studentAbsent` distinct on all four admin consumers — `teacher` 4/4 AR and EN;
   `teacher-performance` 17/18 AR, 17/17 EN.
6. **0** fake saved/sent/deleted/created claims.
7. **188** in-body relative links all resolve; **0** `href="#"`.
8. Quick tiles: **7 real links / 0 "soon" badges** in both locales.
9. **0** email/phone-shaped values outside the `teacher-profile` own-contact allowance.

## 3. Test artifacts — the true final inventory

| Kind | ID | Notes |
|---|---|---|
| Declared supersession | **S45-1** | teacher-portal body-anchor pin → exact 8-destination register. Falsified by M45-04, M45-07. |
| Declared supersession | **S45-2** | teacher internal-page zero-form-controls rule, narrowed to `teacher-library` only. Falsified by M45-12, M45-13. |
| Additive guards | **G45-1 … G45-8** | Full register in `mutation-ledger.md`. |

**Correction:** earlier notes described "all 8 Spec-045 guards" (S45-1, S45-2 + G45-1…G45-6). That
was the session-4 inventory. The final inventory is **10 registered test artifacts**: 2
supersessions + **8** additive guards. **G45-7** (drawer opener→target mapping — 38 openers,
0 dangling) and **G45-8** (the consolidated 7-section fail-loud Teacher-domain census that actually
satisfies T063) exist in the pushed bytes but were undocumented until this reconciliation.

### Changed-test diff, measured

`git diff --numstat 722be1c..HEAD -- app/tests`:

| File | Added | Removed |
|---|---:|---:|
| `tests/smoke/run.cjs` | **524** | **6** |
| `tests/screenshots/capture.cjs` | **62** | **0** |

- **77** added `ok(` assertions.
- **3** removed assertion lines, all accounted for: 2 are S45-1's superseded pair, 1 is S45-2's
  superseded blanket rule. **No undeclared deletion.**
- **0** skips, `.skip`, `xit(`, `todo(`.
- **0** empty `catch` / `.catch(() => {})` in executable test code. A literal grep reports 2 hits;
  both are **comment lines** in the G45-6 block documenting the M45-12 optional-catch-binding fix.
  G45-6 itself scans for the pattern at runtime and reports **0 flagged blocks**.
- `capture.cjs` is **strictly additive** (0 removals): **34** `sp045-` variant rows.

## 4. FR-031 closure evidence

`avgUtil` and its percentage card are gone from `pages/teachers.js`. The third summary card is a
plain count of teachers in the authored `high` workload band. Verified four ways: **source**
(`avgUtil` absent, `.util` unread, no `reduce(...)/length` mean — the Spec-045 diff on that file is a
single hunk), **rendered output** (`percentChars=0` in both localized summary rows, measured this
run), **visual** (AR 390px renders «٢ معلّمون بحِمل مرتفع» and exactly two teachers carry «حِمل
مرتفع», so the count is truthful), and **mutation** (M45-17 reintroduces the mean; G45-2 fires three
causal assertions).

## 5. Audit- and guard-accuracy corrections (FR-061)

A guard that reports a wrong result is itself a defect, so each is recorded rather than quietly
repaired:

- `/SAR/i` matched the persona name **"Sara"** → currency codes are now `\b`-anchored.
- Arabic student-absence renders «غياب طلاب», not only the shadda form → both orthographies accepted.
- "no computed rating" / "display only, no scores" are the product **truthfully denying** a ranking →
  negated forms excluded.
- «يتم الحفظ بعد ربط الخادم» is an **honest gate** containing «تم الحفظ» as a substring; JS `\b` is
  ASCII-only → an Arabic-letter lookbehind was required.
- G45-4's first version asserted `[data-theme="dark"]` and went RED against an intact stylesheet —
  the minifier strips attribute-selector quotes. Made quote-agnostic.
- G45-4's anchored form (session 4): the original pattern also matched the
  `:not([data-theme=dark])` system fallback, so deleting the explicit rule left the guard satisfied.
  Now anchored to a rule start, with the fallback pinned separately.
- G45-6's empty-catch regex required a parameter list, so a bare ES2019 `catch {}` slipped through
  (M45-12). Parameter list made optional; the guard skips its own block.

## 6. Corrections applied during this reconciliation

| # | What | Evidence | Gate impact |
|---|---|---|---|
| 1 | **A partly vacuous assertion inside S45-2.** The library empty state is toggled by CSS `display` and **never** sets the `hidden` attribute, so `emptyShown = !n.hidden` was true whenever the node merely existed. | Probe at four query states: initial → `display:none`; `worksheet` → 1 card, `display:none`; `zzzznomatch` → 0 cards, `display:block`; reset → 3 cards, `display:none`. | Replaced with a computed-style test **and** a new assertion that the empty state is HIDDEN while results exist. `run.cjs` **+11 lines, 0 assertions removed**. **Smoke re-run → PASS, exit 0.** Test-only: no app source touched, so build/parity/impact/a11y/screenshots are unaffected — the post-correction build leaves `app/public` at 0 porcelain lines. |
| 2 | **M45-12's recorded RED text named the wrong guard.** | Retained runner log `m12.log` reads `G45-6: guard G45-2 contains an empty catch`. `tasks.md` T085 already said `G45-2`; `mutation-ledger.md` said `G45-5`. | Documentation only. Corrected in `mutation-ledger.md`. Outcome unchanged: M45-12 remains proven causal RED. |
| 3 | **S45-2 was missing from the FR-062 supersession register**, though correctly declared and argued in the test source. | Recovered verbatim from `run.cjs:2055-2106`. | Documentation only. Registered in `mutation-ledger.md`. |
| 4 | **G45-7 and G45-8 were undocumented.** | `run.cjs` live bytes; G45-8 at `:3182+`. | Documentation only. Registered. |
| 5 | **Capture counts were stale** (411 claimed; 34 `sp045-` rows actually present). | `npm run screenshots` → **436 captured · 0 console errors**. | Documentation only. |
| 6 | **The "80-frame" Teacher matrix could not be recovered.** | No retained output supports it. | **Matrix re-run in full** → **64 frames**, byte-backed per-frame table in `screenshot-review-ledger.md`. T060 corrected to the measured value. |
| 7 | **`prt` locale parity was recorded as 663/663.** | Measured **670/670** (session-4 keys landed after that record). | Documentation only. |
| 8 | **A pre-existing dead import was described as "no dead code".** | `button` in `teachers.js:14`, unused since commit `4be3e87`; Spec 045 never touched that line. | Not fixed (cross-spec edit); the claim is corrected. See `executor-review-ledger.md` § "LEAD RULING". |

**Two probe defects found and fixed in the probes, not the product** — recorded for the same reason
as §5: the matrix driver's first empty-state check used `!n.hidden` and produced a false FAIL on the
two "filtered" frames; the inspection probe's first draft counted profile gates with
`[data-disabled-reason]` instead of `.pt-planned .chip.tone-amber` (the selector the protected smoke
guard uses) and produced a false FAIL. Both were defects in the measurement, and both were corrected
before any result was recorded.

## 7. Weighted executor contribution — final

Authoritative table in `assignment-ledger.md` § "FINAL weighted accepted allocation (sessions 2–5)".

**Kimi K3 53 / 82.5 = 64.2%. Claude Opus 29.5 / 82.5 = 35.8%.**

Kimi holds the largest share by a clear margin. **64.2% is marginally below the 65–75% target and is
reported rather than adjusted.** The gap traces to two recorded events, not to any re-allocation: the
lost Batch C+D run (≈28 Kimi units, terminated early by the lead — a lead error) and the D1 watchdog
expiry (the lead authored the AR locale key and G45-2 to leave the tree consistent). Nothing was
relabelled, back-dated, or moved between executors.

---

# HISTORICAL / SUPERSEDED — NOT CURRENT STATE

> Retained for provenance only. **These are mid-run snapshots and do not describe the current state.**
> The current state is §1–§7 above.

**Superseded header (2026-08-02):** "Executor preflight is complete but failed its go/no-go
condition." Superseded by the 2026-08-03 resumed-run preflight, which PASSED on both grounded
analysis and file delivery (`assignment-ledger.md`).

**Superseded — the resumed-run gate table (2026-08-03).** Every row was marked "partial" and the
mutation row read *"4 run, 1 guard hole found and closed; the remaining 12 are outstanding"*. True at
that moment only. Final: 17/17 mutations proven, no partial gates.

**Superseded — the session-4 gate table.** Recorded "65 frames this run", "impact 26 bodies changed",
"11 proven causal RED (M45-01/02/03/04/06/07/08/10/13/15/17)" and "S45-1, S45-2 and G45-1…G45-6 all
active". The impact figure still holds. The mutation figure was end-of-session-4 (final: 17), the
frame figure was that session's interim captures (final matrix: 64), and the guard list predates
G45-7/G45-8 (final: 8 additive guards).

**Superseded — the session-3 gate table.** Recorded locale parity `prt` 663/663 (final **670/670**),
16 bodies changed (final **26**), 40 frames, and "9 of 16 mutations + M45-17".
