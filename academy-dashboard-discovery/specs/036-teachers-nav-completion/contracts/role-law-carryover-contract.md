# Contract — Role-Law & No-Fake Carryover

All standing laws (Specs 016–035) remain BINDING; protected smoke assertions stay **byte-verbatim**. (Plan mirror of `../role-law-and-no-fake-carryover.md`.)

## Role laws (green)
- **Teacher pay-free (GLOBAL):** the fold anchors + the two new tabs carry NO salary/rate/fine/payout/pay figure or vocabulary; teacher-PORTAL ×16 byte-identical; `teacher-performance.html` is the sanctioned admin exempt board (Spec 024 B-07).
- **Family zero-pay / Student child-view:** no family/student surface touched.
- **Admin finance Spec-009 invariant:** no finance body touched; tabs carry no invoice/salary/payout figure.
- **No computed score/rank/chart:** both tabs display counts + categorical labels only; no `<canvas>`.

## No-fake laws (green)
- No fake teacher/category/KPI/monthly persistence or calculation; honest gate wording.
- No backend/API/websocket/db/auth; no external dependency (`package.json` 0-diff).
- No new `data-*` hook / storage key / engine (reuse tabs/drawers/filters).
- No `href="#"`, dead button, raw key, `type=file` (CV=gate), `type=password` (reset=gate), credential/secret.

## Protected assertions (byte-verbatim)
- `payHit`/teacher-pay guards, `payFigure`/`famPay`, child-view guard, finance/settings invariants.
- `nav010` (6 rail cats · finance sub-section · **admin-category admItems===5** · admin-menu 50 · banks placement · planned-truthfulness).
- Spec-026 action-completion, Spec-032 form-completion (incl. trn-* FORM_DRAWERS_032/PICKERS_032/HYBRID_032), 026–035 per-page asserts, Spec-035 Families/Students block.
- Spec-036 additions = a separate additive Teachers block; never edit a protected regex.

## Sanctioned amendment
- The dashboard planned-item probe repoint (teachers → reports/admin/settings). No route-freeze number change.

## Impact-protection expectation
- `teachers.html`/`teacher.html` `#page-body` byte-identical; `teacher-performance.html`/`.en` bodies change (tabs) — the ONE sanctioned body change; portals ×16 + index + all other admin `#page-body` byte-identical; `package.json`/`enhance.js`/`build-html.mjs`/`i18n.js` 0-diff.
