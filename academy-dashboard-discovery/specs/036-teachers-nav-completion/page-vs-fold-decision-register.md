# Page vs Fold vs Deep-link — Decision Register (Spec 036)

| Item | Decision | Reason | Evidence | Expected route | Count | Acceptance check |
|---|---|---|---|---|---|---|
| **addTeacher** / إضافة معلم | **Folded owner (anchor)** | The Add-Teacher form (`trn-add` drawer) already exists on teachers.html; a standalone page would duplicate it. add-family is a page only because family creation is a 5-step wizard — teacher creation is a single flat form. | teachers.js:112 + teacher-actions.js:71 (`trn-add`); Spec 032 FC-22; Spec 033 CS-10 / matrix row 40 / page-vs-deeplink row 17. | `teachers.html` | **0** | Nav anchor → teachers.html; `trn-add` reachable; Save gated; no pay/password. |
| **teacherCategories** / فئات المعلمين | **Folded owner (anchor)** | The teacher-category surface (`trn-categories` drawer: list + Create form + gates) already exists on teachers.html; not hash-addressable, so anchor to the page. | teachers.js:70-84,110 (`trn-categories`); Spec 028 T-K; Spec 032 FC-24; Spec 033 CS-11 / matrix row 41 / page-vs-deeplink row 18. | `teachers.html` | **0** | Nav anchor → teachers.html; `trn-categories` reachable; Save/assign gated; no fake mutation. |
| **sessionsKpi** / مؤشر أداء الحصص | **Fold as a display tab** | Display-only KPI board fits teacher-performance.html (the teacherKpi board); no existing surface; standalone unjustified. Legacy "Classes KPI" had a computed % — NOT reproduced. | teacher-performance.js (display-only board, no tabs yet); legacy `management-class-feedback.md:122-129`; Spec 033 CS-12 / matrix row 43 / page-vs-deeplink row 19. | `teacher-performance.html#view=sessions-kpi` | **0** | Hash opens a display-only sessions-KPI tab; no computed rank/%/chart; no pay. |
| **monthlyPerf** / الأداء الشهري | **Fold as a display tab** | Same host + `tabs()` widget; display-only monthly board; legacy "Monthly Performance" had a computed % — NOT reproduced; does not duplicate Spec 029 feedback engine. | legacy `management-teacher-feedback.md:170-188`; Spec 033 CS-13 / matrix row 44 / page-vs-deeplink row 20. | `teacher-performance.html#view=monthly` | **0** | Hash opens a display-only monthly tab; no computed rank/%/chart; no pay. |

**Net:** 2 fold-anchors (addTeacher, teacherCategories → teachers.html) + 2 display tabs (sessionsKpi, monthlyPerf → teacher-performance.html) → **115 → 115 (0)**. The `tabs()` widget is added to `teacher-performance.html` (its body changes; still count-0).

## Mechanism to add tabs to teacher-performance.html (implementation note, for the plan)
- Wrap the current board (tiles + comparison list + follow-up queue) as the default **Overview** tab; add **sessions-kpi** and **monthly** tabs using the existing `tabs()` widget + `#view=` hash (as on student/course/finance pages). No new hook.
