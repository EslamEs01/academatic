# Contract: Role-Law Carryover — Spec 034

**Binding.** All standing laws (Specs 009/016/021–033) stay green; see `role-law-and-no-fake-carryover.md` (specify phase) for the full list + enforcement anchors. Summary:

- **Teacher pay-free** (payHit/tchPay/PAY28 byte-verbatim) — Control pages carry no salary/rate/fine/payout figure (leads/tasks name teachers only).
- **Family zero-pay** (famPay/payFigure byte-verbatim) — no currency/price figure (leads name courses, never a price).
- **Student child-view** — no student primary-role wording.
- **Finance no-fake-money / Admin finance Spec-009 invariant** — Control pages are non-finance; carry no finance chrome/figure.
- **Settings no-fake-settings** — announcements does NOT duplicate the settings Notifications form.
- **No `type=file`/`type=password`/secret/credential**; **no computed Total/`<canvas>`/`.pdf`/`window.open`/`blob:`**.
- **backendRequired finals**; **no fake persistence/success**; **AR/EN mirrored**; **mobile-390**; **a11y 0/0**; `href="#"`=0; raw-keys=0.
- **Verify**: protected smoke regexes unchanged in the diff; role-law block green; teacher-portal ×16 + family + student + all other existing bodies byte-identical (only shared sidebar gains 5 anchors).
