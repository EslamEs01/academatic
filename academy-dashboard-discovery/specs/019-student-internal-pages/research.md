# Research & Decisions — Spec 019 (D1–D16)

Grounding: [`visual-grounding.md`](visual-grounding.md) (7 areas, gate COMPLETE) · the in-repo Spec 016/017/018 law · direct source reads this cycle: `portal-shell.js` (shell signature `activeId = 'home'`, navItem/navList), `build-html.mjs` (PAGES table + **line 130 calls `portalShellMarkup` WITHOUT activeId**), `fixtures/portal.js` (ROLE_NAV + retained 013/018 slices), the three home modules (incl. the 018 `quickTiles` helper), both locales, `tests/smoke/run.cjs` portal block, `tests/a11y/run.cjs` matrix, `capture.cjs` matrix.

**D1 — Visual grounding sufficiency.** SUFFICIENT for all six pages, verified per area against `visual-grounding.md`: schedule ← `student-timetable-full` · homework ← honest no-direct-evidence note + teacher course-history/F6/task-taxonomy frames · materials ← both library frames (anti-pattern evidenced) · progress ← honest no-direct-evidence note + `management-analysis-student-full` (admin-side analytics; charts stay out) + subscriptions rows · history ← history-filter + teacher course-history frames · profile ← profile-500 + `student-profile-edit-full` (fixes the exact 3 gates) · admin rhythm ← `academy-dashboard.png` + compact table page. The no-legacy-student-role reality is documented. **No amendment needed.**

**D2 — Page module architecture.** Six independent page modules `src/js/pages/student-{schedule,homework,materials,progress,history,profile}.js`, each exporting `renderStudent<X>()` returning baked band HTML for `#page-body` (identical to every existing portal page module). Per-page band recipe: compact `pt-home-head` header (title + one-line purpose) → summary band → 1–3 short content bands → labeled gates → closing `.pt-note`. ≤2 desktop screens each.

**D3 — Shared helper vs six copies.** **ONE small shared component `src/js/components/portal-page.js`** (a sanctioned ADDITION to the allowed list, justified here): exports the portal-side primitives the 018 homes currently duplicate (`secHead`, `kpiCard`/`kpiRow`, `plannedCard`, `gateNote`, `pageHead`) for use by the SIX NEW modules only. Rationale: 018 duplicated helpers across 3 files because its file scope forbade a shared module; duplicating across 6 more files (9 copies) is the drift risk the Spec-018 clean-code review explicitly flagged. The 018 home modules are NOT migrated (preserves their built identity); role-agnostic name so Specs 020/021 reuse it. Additive file, portal namespace, zero frozen-file impact.

**D4 — Build registration.** `build-html.mjs` (allowed): six `renderStudent<X>` imports + six PAGES entries `{ base: 'student-<x>', shell: 'portal', role: 'student', personaKey: 'data.stud.a.name', activeId: '<navId>', titleKey: 'prt.title.stu<X>', render }` + **the ONE line-130 amendment**: `portalShellMarkup({ role: p.role, personaKey: p.personaKey || '', bodyHTML: p.render(), activeId: p.activeId || 'home' })`. Byte-neutral for all 25 existing pages (portal entries carry `activeId: null` → `null || 'home'` = the current default; admin pages don't take this branch) — the hash audit proves it. Total build-html touch: 6 imports + 6 entries + 1 amended line, enumerated in the impact contract.

**D5 — Nav flip.** Exactly six one-line edits in `ROLE_NAV.student`: `status: 'planned'` → `'implemented'` (schedule/homework/materials/progress/history/profile). Order/labels/icons/pages FROZEN. Family/teacher registries byte-untouched (grep-proof at G-audit). Blast radius: every student-shell page re-bakes its nav (that is the point); family/teacher/hub/family-child pages don't consume the student registry → byte-identical.

**D6 — Fixture structure.** ONE additive group in `fixtures/portal.js`: `STUDENT_PAGES = { homework: { kpis(authored trio: pending/inProgress/reviewed counts 2/1/2), records[5] (the RETAINED hw1–hw3 re-referenced + 2 NEW authored reviewed/done records w/ teacher-note keys) }, materials: { groups: byCourse c1/c3 (the RETAINED mat1–mat3 re-referenced + 1 NEW per-course item) }, progress: { signals: [2 authored teacher-signal keys] }, history: { records: (the RETAINED history h1(out1)/h2/h3 re-referenced + 2 NEW authored records w/ outcome-shaped fields) }, profile: { prefs: [lang/theme/contact chips], gates: ['photoUpload','profileSave','passwordChange'] } }`. Schedule needs NO new slice (SESSIONS_FULL + SCHEDULE_WEEK sara-proxy + existing keys). Rules: additive only; ZERO deletion/rewording of retained 013/018 slices; every figure consistent with st1 (attended 9 · math 78% · prog 41% · streak 5); no new domain facts.

**D7 — Locale namespace.** Additive, key-mirrored AR/EN: `prt.title.{stuSchedule,stuHomework,stuMaterials,stuProgress,stuHistory,stuProfile}` (declared shared-title-map additions) · `prt.stu.pg.{sched,hw,mat,prog,hist,prof}.*` (page headers/hints/section titles/gate lines/chip labels/empty lines) · `data.prtStuPg*` (authored record strings). Existing `prt.stu.*` keys are REUSED verbatim where the copy fits (todayTitle/weekHint/histTitle/att.* etc.) — reuse never rewords.

**D8 — activeId behavior.** Per-page `activeId` = the page's nav id, delivered via D4's pass-through. Shell (`portal-shell.js`) UNTOUCHED — it already accepts any id. Each internal page: its item `is-active`+`aria-current="page"` ×2 instances; home renders as a plain link. Home keeps `activeId 'home'`.

**D9 — Anchor inventories (smoke-pinned).** Per STUDENT page (all 7, both languages): shell anchors (outside `#page-body`, non-hash) = 7 nav targets ×2 instances + hub×3 = **multiset 17**; unique set = {7 student pages + portals}; `navCurrentHrefs` === 2×self; `plannedNavAnchors` === 0; `navListAnchors` === **7** (all implemented). Body anchors: **internal pages === 0** (the sidebar owns navigation; zero sanctioned exceptions) · **student home === 6** (the honesty-required quick-tile links, exact set = the six sibling pages, language-correct, each once — see D13). Family/teacher/hub/family-child inventories UNCHANGED (their branches stay byte-verbatim).

**D10 — Smoke amendment scope.** ONE amendment, enumerated: (a) PAGES +6 → 63 loads; PORTAL_PAGES +6; VALID_FILES follows PAGES automatically; (b) NEW student-internal branch: per-page shell/role check, active-nav ===2×self, navListAnchors===7, shell multiset 17, bodyAnchors===0, formControls===0, tables===0, raw-key/pay-figure hygiene, per-page specifics (profile: exactly 3 amber backendRequired gates + zero forms; homework: ≥5 records + state sections; materials: ≥4 cards + no hero; progress: bars ≥2 + zero charts is structural; history: ≥5 records); (c) student HOME branch re-scope: `bodyAnchors === 6` w/ exact sibling targets (replaces ===0), navListAnchors 1→7, shell multiset 5→17, planned-card counts unchanged (2); (d) `expPlanned` map gains the six new pages (values pinned per page at implementation; profile expects its 3 gates as `.pt-planned` cards → plannedBackend===3); (e) family/teacher/hub/family-child branches + payHit + family zero-pay regex + admin asserts **BYTE-VERBATIM**; (f) 390px + ceiling probes extended per D11.

**D11 — Ceiling asserts for internal pages.** YES — extend the smoke ceiling probe to the six internal pages: scrollHeight @1366×768 within **[500, 2200]** (lower floor than the homes' 900 because single-purpose pages are legitimately shorter; same ceiling so the endless page can never return anywhere in the student app). Homes keep [900, 2200] byte-verbatim. Recorded heights per page land in REVIEW.md.

**D12 — Identity target.** 63 built files. Changed: 12 NEW + `student-portal(.en).html` (nav region + quick-tiles band) = 14 → **49/63 hash-identical** (40 admin + index + hub pair + family home pair + family-child pair + teacher home pair + gallery et al. — precisely: all 49 non-student-non-new files). `public/assets/*` mirrors regenerate (reported as asset churn). The D4 line-130 amendment must produce zero hash drift on the 49 — asserted by the audit.

**D13 — Student home after the flip (the honesty finding).** The 018 `quickTiles` helper bakes `.pt-qtile is-planned` + «قريبًا» pills UNCONDITIONALLY from the registry. After the flip those pills would claim six LIVE pages are "coming soon" — an honesty-law violation. Therefore **"body byte-equal" is impossible without lying**, and the spec's "if possible" clause resolves to: `student-portal.js` quickTiles becomes status-aware — `implemented` → a real `<a class="pt-qtile">` language-correct link; `planned` → the existing planned tile byte-pattern. Home body anchors 0→6 (D9); every other band byte-equal (the diff is confined to the quick-links band; the KPI/now/core/preview bands and all body asserts otherwise unchanged). Family/teacher homes use their own registries (all-planned) → their quickTiles output is byte-identical even though the helper text in THEIR files is untouched (each home carries its own copy — only the student copy is edited). Recorded as the honest deviation from Decision 4.

**D14 — Pay/zero-pay protection.** Structural: no teacher/family/family-child source file is in the allowed list; the six new modules + fixtures + locales are authored against the extended token set (checked at authoring + G-audit greps incl. comments); the byte-verbatim smoke lines (payHit, family zero-pay ×2, family-child branch) re-run green on every load. Student pages carry zero pay/currency register by authorship (and the smoke pay-figure hygiene check from D10(b) pins it).

**D15 — Capture matrix.** Additive rows: each of the six pages AR-light-desktop + AR-light-mobile (12) · one AR-dark sample (progress — densest tokens) · one EN sample (schedule) · student home AR-light-desktop refresh (the tiles-now-links proof) · unchanged proofs = existing family/teacher/family-child/admin frames re-captured byte-stable. All full-page; zero console errors required.

**D16 — Coverage annotations.** Append-only: (a) 016 `legacy-to-new-coverage-matrix.md` gains the Spec-019 delivery annotation (the STUDENT section's net-new-value claim → six delivered pages; guardian-proxy capabilities cited per page); (b) 018's displacement map rows marked re-homed: achievements+celebration→progress · full week→schedule · materials+matDownload→materials · full history+fullHistory→history · profile+editNote→profile · hwSubmit→homework (the retained keys now render again — none may be retired); (c) CLAUDE.md SPECKIT pointer + README student-app section + the Django registry note.
