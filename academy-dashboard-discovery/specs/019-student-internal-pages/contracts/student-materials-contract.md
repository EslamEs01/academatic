# Contract — student-materials (Spec 019)
Bands: pageHead · per-course groups (c1 math, c3 programming) of material cards `{type medallion + type chip + title}` · download/open backendRequired gate (the re-homed matDownload) · truthful empty where authored-thin · note. Items: RETAINED mat1–mat3 re-referenced + NEW per-course items (STUDENT_PAGES.materials). FORBIDDEN: the marketing hero (the legacy library anti-pattern — evidenced in both library frames), fake file/download anchors, tables, search theater. Course/topic filters ONLY if genuinely narrowing baked rows via the existing closed hook set — else omitted (omission chosen; groups make filters unnecessary at this data size).
