# Contract — Student Page Shell (Spec 019)
All six pages render inside Shell v2 via the build PAGES table: `{ shell:'portal', role:'student', personaKey:'data.stud.a.name', activeId:'<navId>', titleKey:'prt.title.stu<X>' }`. `portal-shell.js` UNTOUCHED. The ONE sanctioned `build-html.mjs` touch = 6 imports + 6 entries + the line-130 pass-through (`activeId: p.activeId || 'home'`) — byte-neutral for all 49 existing files (hash-audited). Per page: its nav item `is-active`+`aria-current="page"` once per nav instance (===2 total); home a plain link; drawer + aside both carry the full 7-anchor registry; shell-anchor multiset 17, unique {7 pages + hub}. Body anchors === 0 on every internal page (the sidebar owns navigation — zero sanctioned exceptions). sky accent via `data-role="student"` tokens only; admin chrome/selectors never imported.
