# Contract — student-profile (Spec 019)
Bands: pageHead · identity card (avatar · name · level · lifecycle chip) · academic rows (level/subject/course/group/teacher) · safe guardian/family line (guardian name + city only — no contact leakage beyond the existing fam profile registers) · preference/status chips (language/theme/contact — display-only) · EXACTLY THREE backendRequired gates as `.pt-planned` cards: photo upload · profile save · password change (the legacy profile-edit page's exact write surface) · note. HARD PINS (smoke): zero `<form>/<input>/<select>/<textarea>`; plannedBackend === 3 on this page; body anchors === 0. FORBIDDEN: fake save/password/photo controls, editable-looking fields. Grounding: profile-500 + `student-profile-edit-full.png`.
