# Contract — Honesty / backendRequired (Spec 019)
The four honest action classes only. ZERO fake: live join · chat send · notifications · homework submit/upload · material download/open · profile save · password/security update · attendance write · payment. Every unavailable action = a LABELED availability chip (the frozen Spec-008 vocabulary via `availabilityChip`) on a `.pt-planned` card or `.pt-note` gate line — never an anchor, never color-only. Zero `href="#"`; zero dead links; zero dead filters; quick-tiles on the home MUST tell the truth post-flip (implemented → real links — the D13 finding; a «قريبًا» pill over a live page is a violation). Gate inventory per page pinned in each page contract; the profile's 3-gate count is smoke-asserted.
