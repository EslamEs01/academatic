# Contract — student-history (Spec 019)
Bands: pageHead · session-record card list (REAL `out1` outcome ref FIRST, then RETAINED h2/h3, then 2 NEW authored records) — each `{day/date · course · teacher · labeled outcome chip · summary line · homework-note line · attachment annotation where authored}` (the F6 record shape) · display-only period chips (clearly non-interactive `.pt-tag`s, no filter semantics) · truthful empty beyond the authored window · note. FORBIDDEN: dead filters (display-only chosen over baked filtering — 5 records don't warrant filter machinery), fake modals/saves, select-student forms (single-persona page). Grounding: history-filter + teacher course-history frames (the record shape + labeled status chips).
