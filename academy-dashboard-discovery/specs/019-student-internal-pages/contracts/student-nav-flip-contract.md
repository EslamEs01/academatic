# Contract — Student Nav Flip (Spec 019)
EXACTLY six one-line edits in `ROLE_NAV.student` (`fixtures/portal.js`): `status: 'planned'` → `'implemented'` for schedule/homework/materials/progress/history/profile. FROZEN: entry order, ids, labelKeys, icons, `page` routes. FORBIDDEN: any edit to `ROLE_NAV.family` (1 implemented + 7 planned) or `ROLE_NAV.teacher` (1 + 6) — byte-untouched, grep-proven at the G-audit. Smoke pins: student pages navListAnchors===7 & plannedNavAnchors===0; family/teacher branch asserts BYTE-VERBATIM (their planned counts unchanged). The flip is the Spec-017-designed mechanism: one status line + a built page pair = a live destination; zero shell rework.
