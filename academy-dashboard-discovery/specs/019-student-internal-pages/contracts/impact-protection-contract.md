# Contract — Impact Protection (Spec 019)
PROTECTED BYTE-IDENTICAL (49/63): 40 admin files · index.html · portals hub pair · family home pair · family-child pair · teacher home pair. CHANGED (14): the 12 new student files + `student-portal(.en).html` (nav region + the status-aware quick-tiles band ONLY; all other bands byte-equal). `public/assets/*` regeneration = reported asset churn, not page change. SOURCE scope: the allowed list in plan.md §Allowed; `portal-shell.js`/`enhance.js`/`nav.config.js`/`package.json`/all family/teacher/admin modules FROZEN (0-line diffs proven). The build-html line-130 amendment must cause ZERO hash drift on the 49 (audited). If any count differs at implementation → report honestly with the exact file list before proceeding.
