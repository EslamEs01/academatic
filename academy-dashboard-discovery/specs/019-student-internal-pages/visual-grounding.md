# Visual Grounding — Spec 019 (Student Internal Pages), recorded 2026-07-04

**Gate status: COMPLETE before specification.** Every frame below was OPENED AND VISUALLY REVIEWED
(this session or the recorded Spec-018 gate, re-verified). Two frames were newly opened FOR this
spec's exact surfaces: `student-profile-edit-full.png` (the legacy account-edit forms) and
`admin/management-analysis-student-full.png` (the admin-side student analytics).

**Honest census note:** `output/roles/student/` DOES NOT EXIST. The crawler proved (Spec 016 law,
re-verified on disk) that the legacy system has NO separate student role — the `/student/*` routes
ARE the guardian app, crawled under `output/roles/family/` with `student-*` filenames. All "student"
legacy evidence below is therefore guardian-surface evidence, used as the closest legacy ancestor,
plus teacher-side per-student records where they are the true record shape. This is recorded per the
brief's "document that honestly" instruction.

## Evidence table (the 7 required areas)

| Area | Screenshot/file opened | What was observed | Business idea we keep | What we improve | Impact on the student page |
|---|---|---|---|---|---|
| **Schedule** | `family/student-timetable-full.png` (viewed) | Sat-first day-column week table; EMPTY body in test data; sidebar Schedule item | A weekly view + today emphasis is a real guardian/student need | Dense empty grid → day-grouped agenda CARDS (the sanctioned 013 pattern), truthful rest-day empties, today pinned first | `student-schedule`: now band (today+next) + full week as day groups + honest live/join gate |
| **Homework / tasks** | **No direct legacy student homework page found; nearest evidence used:** `teacher/teacher-course-history-1-full.png` (viewed — per-session homework notes are the real record) + the F6 record shape (summary + homework note per session) + `teacher/teacher-tickets-full.png` (viewed — task-state taxonomy Total/Completed/Pending/In-progress/Overdue) | Homework EXISTS in legacy only as per-session notes written by the teacher; the state taxonomy exists on the teacher side | Give the student a first-class homework surface: state sections (pending/done/reviewed), due chips, course/teacher relation — net-new value grounded in the real record shape | `student-homework`: KPI trio + state-grouped card sections + submit/upload backendRequired gates |
| **Materials / library** | `family/student-library-full.png` (viewed) + `teacher/teacher-library-full.png` (viewed) | Marketing hero ("Education, talents…" + search + rocket art) + All-Categories dropdown + EMPTY list — the landing-page anti-pattern inside a logged-in app | A course/category-organized materials library | Kill the hero; compact type-labeled material cards grouped by course; honest download/open gates | `student-materials`: header + per-course groups + type chips + matDownload gate; zero heroes |
| **Progress / results** | **No student-facing legacy progress page found; nearest evidence used:** `admin/management-analysis-student-full.png` (NEWLY viewed — admin Data-Analysis→Students: stat tiles + per-month/age/language/status/gender/country CHARTS) + `family/student-studentslist-full.png` (viewed — per-child×course progress rows) | Progress/results visibility exists in legacy ONLY as admin analytics + subscription rows; charts are an ADMIN-side language | The student sees THEIR OWN progress: authored KPI tiles, course progress BARS (the sanctioned `.pt-bar`, never charts), attendance trio, achievements — display-only, zero computed | `student-progress`: KPI row + course bars + attendance trio + achievements/celebration (the RETAINED 013 fixtures re-homed here) |
| **History / classes summary** | `family/student-student-history-fillter-2-full.png` (viewed — the Select-Student→Submit history filter) + `teacher/teacher-course-history-1-full.png` (viewed — session rows: date/time + labeled status chip (Waiting/Admin Cancel) + teacher + subject + duration + per-row action) | The per-session record: date/time/teacher/subject/status + the summary/homework note pair (F6) is the core history artifact | Single-persona page needs NO select-student form; card list with labeled outcome chips, summary + homework note per record, truthful empty when thin | `student-history`: full history list (the RETAINED `STUDENT_PREVIEW.history` + real out1 outcome ref, extended by authored records) + display-only period chips |
| **Profile / child details** | `family/student-profile-full.png` (viewed — **500 Server Error**) + `family/student-profile-edit-full.png` (NEWLY viewed — avatar upload (JPG/GIF/PNG ≤1MB) + First/Last name + E-mail + Save; separate Change-Password card (old/new/confirm + Save)) | Account identity + editability + password change are real legacy capabilities (the edit page worked; the view page was broken) | Display-only identity/course/group/teacher/guardian card + preferences chips; the THREE legacy write actions (photo upload · profile save · password change) become labeled backendRequired gates — never fake forms | `student-profile`: identity card + academic details + guardian line + gates; zero form controls |
| **Admin compact reference** | `design-references/academy-dashboard.png` (viewed) + `admin/management-categories-families-full.png` (viewed) | The approved rhythm: compact header · KPI row w/ icon medallions · section headers · short filtered table w/ LABELED status chips · drill-down cards · honest UI states; one-card compact list pages | Everything — the target rhythm for every internal page | Re-expressed in `.pt-*` portal tokens with the sky accent; never admin selectors/chrome | All six pages: compact header → summary band → short content bands → gates; ≤2-screen discipline |

## Standing conclusions carried into the spec

1. The **admin-side analytics frame confirms charts stay OUT of role apps** (and out of scope entirely per the standing no-chart law); student progress speaks in authored bars/tiles.
2. The **profile-edit frame fixes the exact gate list** for `student-profile`: photo upload, profile save, password change — three backendRequired gates, matching the legacy write surface one-to-one.
3. The **no-student-role reality** means Spec 019 is largely *net-new value with legacy-shaped records* (Spec 016 already classified it this way: "net-new value beyond legacy, grounded in guardian-proxy capabilities").
4. The compact discipline from Spec 018 (ceiling-guarded homes) extends to internal pages as a DESIGN rule (each page ~1–2 screens); the smoke ceiling probe stays scoped to the three HOMES unless the plan decides to extend it — an explicit plan-time decision.
