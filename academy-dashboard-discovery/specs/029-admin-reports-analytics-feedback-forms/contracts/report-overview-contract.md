# Contract — Report Overview

**Guarantee**: `reports.html` overview/catalog/detail stay display-only; filters real static; export/print honest gates.
- Overview tiles = authored roll-ups == underlying row counts (no runtime aggregation).
- Category filter/search = `data-filter` via `filterBar` (client-side facet).
- Detail "more" links = real deep-links.
- Print reclassified (R-G) to disabled-with-reason; Export CSV/PDF/Share stay gates; Schedule stays confirm.
**Verify**: reports loads AR/EN; filters facet; no chart; no computed metric; Print = honest gate.
**Fail if**: a computed metric/chart appears; Print fires a fake success.
