# Correction Backlog for Spec 024 — from the Full Legacy Coverage Audit 000–022

**Title**: Prioritized, evidence-backed correction backlog Spec 024 can implement directly
**Date**: 2026-07-06
**Baseline**: branch `feature/012-role-portal-foundation`, HEAD `837b0c1`; Specs 020/021/022 committed; 77 public HTML files.
**Inputs used**: `coverage-matrix.md`, `missing-capabilities-register.md` (M-01…M-16), `extra-or-drift-register.md` (X-01…X-49), `design-quality-register.md` (D-01…D-15), `role-model-consistency-audit.md` (F-00-1), `current-vs-legacy-map.md`.

**Scope law for Spec 024**: corrections only — respect every binding law (teacher pay-free GLOBAL, family zero-pay, no fake actions, honest gates, static HTML-first, no deletion of useful work without a preservation plan, student demoted-not-deleted, admin/family/teacher primary). Any change touching a pinned `#page-body` extraction hash ships with a **declared hash supersession + ONE sanctioned smoke amendment** (the 022 family-child precedent), never a bypass. Admin/index identity byte-preservation held.

Priorities: `Must fix in 024` · `Should fix in 024` · `Can schedule later` · `Do not fix / intentionally excluded`.

---

## Backlog

| Backlog ID | Problem | Evidence | Why it matters | Recommended correction | Affected files/pages | Risk | Acceptance criteria for 024 | Priority |
|---|---|---|---|---|---|---|---|---|
| **B-01** | Leftover pre-021 Student-primary wording «لوحة الطالب — النسخة الأولى» + «هذه لوحتك الدراسية…» on 6 of 7 child-view pages (confirmed F-00-1) | `role-model-consistency-audit.md` F-00-1; `agent-findings/00-main-session-grounding.md`; `ar.prt.js:297-298`, `en.prt.js:294`, `app/public/assets/locales/ar.prt.js:297`; built: `student-portal.html:393`, `student-homework.html:397`, `student-history.html:344`, `student-profile.html:361`, `student-progress.html:409`, `student-materials.html:341` (+`.en`) | The corrected role model (Spec 021 DEC-001…004) is the project's central verdict; the visible footer still says "your student dashboard", contradicting «عرض الابن» on the same pages | Reframe `noteT`/`noteD` (ar+en) to child-view / guardian-addressed wording («عرض الابن — النسخة الأولى» + guardian-addressed description); rebake the 6 affected pairs + home; DECLARE supersession of the 019/022 `#page-body` extraction hashes for the 5 hash-pinned internals; re-pin the affected smoke assertions — ONE sanctioned amendment | `ar.prt.js`, `en.prt.js`, 6 `student-*.html` pairs + assets locale copy; `tests/smoke/run.cjs` | Low (locale-key + rebake; precedent = 022 family-child hash supersession) | `grep -RIl "لوحة الطالب" app/public/student-*.html` returns **zero**; `grep "student dashboard" app/public/student-*.en.html` zero; 6 extraction hashes re-recorded; smoke green | **Must fix in 024** |
| **B-02** | Admin RBAC **Locations** permission group has no owner row in the 57-row inventory or the 026–031 plan | `coverage-matrix.md` admin row "Locations"; `missing-capabilities-register.md` M-04; `agent-findings/05-admin-coverage.md` Risk 1; `output/roles/admin/screenshots/management-admins-permission-6-full.png` (Show/Add/Edit/Delete Locations) | The ONLY legacy admin capability with no owner in an otherwise-complete map; leaving it unassigned lets it slip past 032 | Record an ownership DECISION only (no build): most natural home is a display slice inside 031's settings/general (legacy general settings already carries Country/City/Timezone/Address). Add one register row + assign owner `031` | Spec-024 decision record; (future build in 031) | Low (decision + register row) | A written owner decision exists; the 57-row inventory becomes 58 with Locations owned by 031; 032 no-missing audit passes | **Must fix in 024** |
| **B-03** | Header notifications capability (bell + "See All Notifications" + badge) on all 13 family pages and every teacher page — no current surface OR labeled gate | `missing-capabilities-register.md` M-01; `agent-findings/06` A1 + Risk 2; `agent-findings/07` R7; `output/roles/family/pages/student-home.md` (Buttons/Badges) | The one true cross-cutting MISSING capability; guardians/teachers lose the "something needs attention" signal; 032 will flag it if unowned | Add ONE honest `pt-guide` backendRequired gate (or a futures-register entry) per role app — never a fake bell/count | family + teacher portal surfaces or a shared futures register | Low (honest gate, no engine) | Notifications appear as a labeled backendRequired gate or an explicit futures-register row in both apps; zero fake counts (grep no numeric badge) | **Must fix in 024** |
| **B-04** | Teacher live `session-class-room` page was NEVER truly captured (crawler redirected to `/teacher/home`); currently unverifiable | `missing-capabilities-register.md` M-05; `agent-findings/07` R6; `coverage-matrix.md` teacher row (unclear-needs-review); `output/roles/teacher/pages/teacher-session-class-room-mq-2.md` | Spec 025 would design the teacher day surfaces partially blind; the page must not be counted covered OR excluded until seen | Order a fresh targeted crawl during a LIVE session before 025 finalizes teacher day surfaces; record the result | crawler config / a new capture; Spec-024 record | Low (a crawl order + record; no app change) | A live-session capture exists (or a written decision that the real room is future-backend regardless); the matrix row moves off `unclear-needs-review` | **Must fix in 024** |
| **B-05** | Teacher library/materials has NO visible planned gate anywhere in the current teacher app (6 nav «قريبًا» items; library absent) | `missing-capabilities-register.md` M-03; `agent-findings/07` R4; `PORTAL` TEACHER_PREVIEW.materials + matUpload retained vs ROLE_NAV.teacher (no library item); `output/roles/teacher/pages/teacher-library.md` | Fixtures are retained (nothing lost), but the capability is invisible — invisible ≠ honestly gated; contradicts the "every capability honestly gated" contract | Record a decision NOW (owner 025): either add a `library` «قريبًا» item to ROLE_NAV.teacher or fold materials into an owned 025 page | `PORTAL` (ROLE_NAV.teacher) or a 025 page; Spec-024 record | Low (decision; possible one nav item) | A written 025 decision exists; if a nav item is added it renders as a non-anchor «قريبًا» button (`plannedNavAnchors===0` holds) | **Should fix in 024** |
| **B-06** | Teacher-side chat has no gate and its only sequenced owner is an ADMIN spec (026 preview) | `missing-capabilities-register.md` M-02; `agent-findings/07` R5; `specs/015-teacher-dashboard/spec.md` (T13); `M016/future-spec-sequence.md` line 13 | The teacher persona currently has ZERO trace of messaging; ambiguous ownership | Record a decision: either ROLE_NAV.teacher gains a chat «قريبًا» item in 025, or the teacher-side exclusion is explicitly registered (admin preview stays 026). Message-SEND stays a backendRequired gate (send-form unconfirmed in crawl — never invent fields) | `PORTAL` or the exclusion register; Spec-024 record | Low (decision) | A written decision exists; no invented send form; if gated, honest «قريبًا» non-anchor | **Should fix in 024** |
| **B-07** | Teacher home's sanctioned performance anchor exits into the ADMIN shell whose nav rail carries `finance.html` + الرواتب labels one click away (pay-free contract rule 3 letter vs spirit) | `missing-capabilities-register.md` M-09; `extra-or-drift-register.md` X-39/X-44; `agent-findings/07` R1; `teacher-portal.html:378` → `teacher-performance.html:354,356-368`; body pay-free asserted `run.cjs:548-561` | The persona experience is teacher home → one click → a shell showing pay-NAMED navigation; sanctioned + smoke-pinned today, but the contract has no written exemption | Record an explicit written pay-free-contract exemption for the pre-existing Spec 007 admin board NOW; schedule (025) repointing the home link to the real `teacher-reports` internal + demoting the admin board link to admin-only | `M016/contracts/teacher-pay-free-global-contract.md` (exemption note via 024); later `PORTAL`/`teacher-portal.html` in 025 | Low (documentation now; retarget in 025) | A written contract exemption exists citing the Spec 007 provenance; 025 backlog item created to repoint the link | **Should fix in 024** |
| **B-08** | Honest teacher exclusions (notifications, "Add shortcuts" widget) are correctly not-faked but UNREGISTERED; 032 would flag them | `missing-capabilities-register.md` M-10; `agent-findings/07` R7; `output/roles/teacher/pages/teacher-home.md` lines 67-69; `teacher-salary.md` form 2 (`/teacher/shortcuts`) | 032's no-missing audit needs every honest exclusion NAMED so it isn't counted as an unexplained gap | Add both to the 024 exclusion register as future-backend personalization (register entry only, no build) | Spec-024 exclusion register | None (documentation) | Both appear in the exclusion register with the no-persistence law cited | **Should fix in 024** |
| **B-09** | Two current child-view surfaces have thin/authored grounding not yet documented: student-progress achievements band (no legacy twin) and student-homework (no `/student/homework` route) | `extra-or-drift-register.md` X-29/X-30; `agent-findings/09` narrative + Risk 2; `student-progress.js`, `student-homework.js` | Fine under the capability-checklist law, but every future audit will re-flag them until provenance is recorded | Add one-line provenance notes: student-progress achievements = authored joy layer; student-homework state derives from session outcomes (honest hwSubmit gate already present). NO page change | `student-progress.js`, `student-homework.js` header comments (or the 024 record) | None (documentation) | Provenance notes exist; both classified `useful-but-needs-better-grounding → grounded-documented`; audits stop re-flagging | **Should fix in 024** |
| **B-10** | Spec 022 day rail visually dropped the pre-022 prep-hint + rich per-session student counts (possible no-deletion-law friction — verify moved vs deleted) | `extra-or-drift-register.md` X-49; `design-quality-register.md` D-01; `app/screenshots/before-022/teacher-portal__ar__light__desktop.png` vs current | The only INWARD drift signal (current-vs-current); if copy was deleted rather than moved it brushes the no-deletion preference; also the biggest visual-density gap | Verify against `specs/022-…/contracts/`; if moved, record where and close; if deleted, restore the copy inside the rail card body (also resolves D-01 density) | `specs/022-…/contracts/`; later rail CSS/content in 024 visual pass | Low-Med (verify first; restore is pure-CSS/content) | A written moved-vs-deleted determination exists; if deleted, rail cards regain a second content column; density gap D-01 addressed | **Should fix in 024** |
| **B-11** | Visual-density / living-uplift debt across internals (empty-heavy rails D-01, stat-void tiles D-05, underfilled week strip D-09, family-children static D-04, dark hero wash D-06, hub empty slot D-08, schedule hierarchy D-07, delight gaps D-10/D-11/D-12, mobile topbar D-13) | `design-quality-register.md` D-01,D-04–D-13; screenshots cited per row | The product owner cares strongly about visual quality; one focused pure-CSS pass on the shared living layer resolves D-01/D-04–D-09 together with low regression risk | A single Spec-024 visual-density pass on the additive living CSS layer: second rail column, compact stat tiles, 7-cell week strip, storyRow for family-children, role-tinted dark hero tokens, 2-up hub row, delight touches. Rows touching pinned bodies (D-02/D-04/D-05/D-09) ship with hash supersession | `app.css` living layer + affected `#page-body` where pinned; smoke re-pin where needed | Med (touches multiple surfaces + some pinned bodies) | Each addressed D-row has a before/after screenshot; pinned-body changes carry declared hash supersession; reduced-motion + closed-hook-set held; smoke green | **Should fix in 024** |
| **B-12** | Scanner-hygiene: EN locale comment carries "pay" twice (teacher-carrying locale); family copy "everything's just fine 🌿" trips a word-bounded `fines?` grep | `extra-or-drift-register.md` X-42/X-43; `agent-findings/07` R2/R3; `en.prt.js:179-180`, `en.prt.js:339` | The pay-free contract's letter is "copy AND comments"; the comment is the only word-bounded source hit; the "fine 🌿" collision risks a future false-red | One-line rewords: match the clean `ar.prt.js:183` phrasing for the comment; reword "all good 🌿" for the family empty state | `en.prt.js` (+ built assets copy) | None (comment/copy hygiene; no rendered teacher change) | `grep -nE "\bpay\b" en.prt.js` teacher-carrying-comment hit removed; `fines?` grep over family copy clean | **Can schedule later** |
| **B-13** | `FUTURE_ROUTES` documentation map incomplete (14 of 29 planned nav ids mapped) | `extra-or-drift-register.md` X-46; `agent-findings/05` Risk 2; `nav.config.js:139-145` | Cosmetic (map is documentation-only) but each of 026–031 would extend it piecemeal → drift | Pre-fill the map in one pass (timeConverter, publicHoliday, scheduledActions, familyCategories, scheduleSearch, addTeacher, sessionsKpi, monthlyPerf, certificateRequests + 6 settings ids) | `nav.config.js` (documentation map only) | Low (documentation map; guarded by the build-time dead-link check) | All 29 planned ids have an intended route entry; build-time no-dead-link guard still green | **Can schedule later** |
| **B-14** | Gallery dev-infrastructure page could read as product drift if reachable from any role nav | `extra-or-drift-register.md` X-20; `agent-findings/09` Risk 1; `gallery.html`, `gallery.js` | Not a product surface; must never be linked from a role nav | 032-final-qa machine-check: no role page links to `gallery.html`; optional subtitle "design-system preview — not a product page". Keep, HIDE, do not remove | `tests/smoke/run.cjs` (032 check); optional `gallery.html` subtitle | None (QA check; page kept) | 032 asserts zero role-nav anchors to gallery; page retained | **Can schedule later** |
| **B-15** | Invoice-amount boundary needs restating so a future pass doesn't "fix" admin finance into breaking the Spec 009 body invariant — or copy amounts onto family/teacher surfaces | `extra-or-drift-register.md` X-47; `agent-findings/05` Risk 6; `finance.html` (24 authored «ريال» literals) | The blanket "zero pay figures anywhere" phrase coexists with sanctioned authored admin invoice literals; without a written boundary a future pass could break either direction | Restate the boundary in one sentence inside the 030 spec contract: authored invoice-amount literals may stay on ADMIN finance pages (Spec 009-invariant); salary/payroll/compensation figures may NEVER appear anywhere; family/teacher stay figure-free | 030 spec contract (boundary note via 024) | None (documentation) | The 030 contract carries the one-sentence boundary; no app change | **Can schedule later** |
| **B-16** | `settingsUsers` nav item duplicates the admins/staff family entry (two nav spots for one surface) | `extra-or-drift-register.md` X-45; `agent-findings/05` §a (line 175); `nav.config.js:115` vs `staff` (line 99) | Both are honest planned non-links today (no user harm), but 031 must land ONE staff surface, not two | Record for 031: decide the single staff home; merge the two nav spots. No 024 build | Spec-031 decision (recorded in 024 backlog) | None (both planned non-links today) | A written 031 decision names one staff surface; `settingsUsers` either removed or aliased | **Can schedule later** |
| **B-17** | Family-children carries NO fold-point link (5 drill-downs, zero child-view link) despite the 022 spec draft mentioning per-child fold links | `extra-or-drift-register.md` X-48; `agent-findings/06` Risk 3; `family-children.html` (5 anchors); `run.cjs:1085` (family-child alone at 6) | Must be protected as INTENTIONAL so a future pass doesn't add dishonest per-child links (preview persona is st1 only) | Record as intentional (do NOT "fix"); revisit only under future-backend if real per-child credentials ship | Spec-024/032 record | None (protective documentation) | The deviation is recorded as intentional; 032 does not flag family-children's missing link | **Do not fix / intentionally excluded** |
| **B-18** | Teacher 4 pay surfaces, family Amount column, chart/score engines, pay-signal leaks, >AR/EN locales, admin payroll figures, `hub.student` displaced key, dead 404/500 routes | `missing-capabilities-register.md` M-10…M-16 (excluded-by-law) + Dead/broken section; `extra-or-drift-register.md` X-41; `coverage-matrix.md` excluded-denominator table | These define the exclusion scope; naming them prevents 032 counting them as gaps AND prevents a future pass "restoring" them | Keep in the exclusion register; carry "do-not-re-import" guard notes into the 026/028/030 contracts; retain the `hub.student` displaced key (zero-deletion law) | exclusion register + 026/028/030 contract notes | None (protective) | All appear in the exclusion register with the governing law cited; `hub.student` retained | **Do not fix / intentionally excluded** |

---

## P0-blocker verdict

**No P0 blockers exist.** Cross-checked against `missing-capabilities-register.md` (severity histogram: 0 × P0-blocker, 6 × P1, 2 × P2, 1 × P3, 7 × excluded-by-law) and `extra-or-drift-register.md` (0 remove; worst finding is the F-00-1 wording leftover). Every P1 item is decision/relabel/register/crawl work sized for Spec 024, not a structural defect. The rebuild is traceable to legacy and is NOT drifting into a different product (drift verdict: NO, `extra-or-drift-register.md` §Drift verdict).

## Spec 024 recommended scope (one list)

1. B-01 child-view wording reframe (Must) — the only page-content change, hash-supersession-guarded.
2. B-02 Locations owner decision (Must).
3. B-03 notifications honest gate/register (Must).
4. B-04 live-room crawl order (Must).
5. B-05/B-06 teacher library + chat gating decisions (Should) — feed Spec 025.
6. B-07 pay-free contract exemption for the Spec 007 board (Should).
7. B-08/B-09 exclusion + provenance register entries (Should).
8. B-10 rail moved-vs-deleted verification (Should) — feeds B-11.
9. B-11 one pure-CSS visual-density/living pass (Should) — resolves D-01/D-04–D-13.
10. B-12…B-18 hygiene, documentation, protective records (Can schedule later / Do-not-fix).

## GO / NO-GO for Spec 025 (Teacher Internal Pages) after 024

**GO — conditional.** Teacher Internal Pages (Spec 025) may proceed after Spec 024, provided 024 closes these teacher-specific prerequisites first:
- **B-04** (live `session-class-room` verified or declared future-backend) — so 025 designs the teacher day surface with evidence, not blind.
- **B-05** (teacher library gating decision) and **B-06** (teacher chat ownership decision) — so 025 knows its full internal-page set.
- **B-07** (pay-free contract exemption recorded) — so the teacher→performance anchor is contractually clean before 025 repoints it.

None of these is a blocker for 024 itself, and the teacher home + pay-free enforcement are already sound (coverage-matrix teacher verdict; `agent-findings/07` three-layer pay-free green). If 024 defers B-05/B-06/B-07, 025 can still start on the pay-free internals (schedule, students, outcomes, reports, profile) and absorb the chat/library/anchor decisions as it lands them — but recording them in 024 is the cleaner path.

## Risks / questions for the product owner

1. **B-01 hash supersession** — confirm the child-view internals' `#page-body` extraction hashes may be re-recorded in 024 (the 022 family-child precedent authorizes this pattern; 024 should re-affirm it explicitly).
2. **B-02 Locations** — confirm 031/settings-general is the intended home, or name another.
3. **B-04 live-room crawl** — is a live-session re-crawl feasible before 025, or should the real room be declared future-backend outright?
4. **B-11 scope** — is a single visual-density pass in 024 wanted now, or should design uplift wait for a dedicated later spec (the audit recommends now: low regression, high owner-valued payoff)?
5. **Certificates family view (M-07/P3)** — should guardians ever SEE issued certificates (future-backend), or is the teacher-request→admin-approve→issue pipeline sufficient? (No legacy family evidence of a certificates surface.)
