# Contract: Final Frontend QA (Spec 016 → Spec 027)

**Status**: Binding pre-commitment for Spec 027.

Spec 027 accepts the frontend as FINISHED only when all eight rules from `future-spec-sequence.md` hold, machine-checked:
1. Coverage-matrix closure (zero planned-spec rows; zero needs-decision).
2. Nav closure (zero planned/disabled without a shipped destination, admin AND role registries).
3. Link integrity (zero href="#", zero dead links, anchors ⊆ sanctioned registries).
4. Action-class audit (zero fake actions; zero form controls in role apps).
5. Pay rules global (teacher family greps zero; family pages figure-free; reports finance-free; payHit lineage green).
6. Quality gates (smoke all loads · axe 0/0 · 390px probe all pages · zero raw keys · zero console errors in captures).
7. Identity lineage (per-spec diffs confined to owned families, re-verified cumulatively).
8. Visual verdicts (REVIEW.md PASS per page family against the freeze's forbidden register).

**Acceptance**: 027 publishes the close-out table; any red cell blocks acceptance; no rule may be deleted or weakened.
