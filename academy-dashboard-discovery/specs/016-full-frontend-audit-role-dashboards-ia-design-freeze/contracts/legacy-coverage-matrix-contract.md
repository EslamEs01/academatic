# Contract: Legacy Coverage Matrix (Spec 016)

**Status**: Binding · Binds `legacy-to-new-coverage-matrix.md` (extends the Spec-012 artifact §§1–9, never edits it).

1. All 178 legacy route templates carry one status from the sanctioned 21-value vocabulary; zero uncategorized; zero needs-decision.
2. Implementation specs cite their matrix rows + screenshot evidence per D8 (route + `output/roles/<role>/screenshots/` path or `docs-only evidence`).
3. Status transitions are one-way honest: planned-spec-0NN → already-delivered (by that spec) · needs-decision may never reappear · exclusions stay excluded.
4. The matrix is the single source for 027's coverage close-out; delivery updates append per-spec delivery notes (the §7/§8/§9 pattern) — never rewrite rows.

**Acceptance**: at each spec's G-audit, its delivered rows are annotated; at 027, zero rows remain in any planned-spec-0NN status.
