# Contract: Role Dashboard IA (Spec 016)

**Status**: Binding on Specs 017–020 · Binds `role-dashboard-ia.md` + `role-dashboard-page-inventory.md`.

1. `student-portal.html` / `family-portal.html` / `teacher-portal.html` are Role Dashboard HOME pages — kept filenames, kept content (deepening only, never deletion), re-hosted in shell v2 by 017.
2. Shell v2 = portal header (role topbar) + flat single-level role sidebar (7–9 items) + mobile drawer; a SECOND shell family — zero admin chrome, forever.
3. Role nav maps are frozen (student 7 · family 8 · teacher 7 items); changes require an explicit amendment in the changing spec.
4. Internal page sets are frozen (student 6 · family 7 · teacher 6); pages may split (amendment) but never merge further; filenames follow `role-*.html` (+ `.en.html`).
5. Sanctioned-anchor registry per portal page: every body anchor must be in the registry; smoke pins it (the Spec-015 exact-target discipline generalized).
6. Hub stays the only entry; no cross-role links; personas stay st1/fam1/sara until real auth (backendRequired).

**Acceptance**: 017–020 each cite this contract; any nav/page-set deviation appears as a named amendment; 027 verifies the registries.
