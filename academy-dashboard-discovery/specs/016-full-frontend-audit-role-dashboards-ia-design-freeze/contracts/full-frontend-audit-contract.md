# Contract: Full Frontend Audit (Spec 016)

**Status**: Binding · Binds `frontend-audit-001-015.md` + `missing-pages-and-gaps-register.md`.

1. All fifteen specs 001–015 carry delivered/owned/deferred/excluded/weakness entries grounded in their folders (all tasks.md 100%-checked, verified).
2. Every finding is registered exactly once with one sanctioned classification; the register's totals reconcile (20 rows; must-fix = 0).
3. No gap may be hidden, softened, or re-classified without amending the register — 027 re-audits against it.
4. The audit's verdict stands: nothing broken/fake/silently-missing; the corrective work is architecture + coverage, not repair.

**Acceptance**: register rows = 20; classification column 100% filled; must-fix count 0; every `move-to-spec-0NN` destination exists in the sequence contract.
