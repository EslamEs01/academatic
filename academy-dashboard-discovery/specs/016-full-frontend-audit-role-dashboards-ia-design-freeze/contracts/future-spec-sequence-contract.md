# Contract: Future Spec Sequence (Spec 016)

**Status**: Binding · Binds `future-spec-sequence.md`.

1. The sequence 017→027 is adopted and ordered; reordering is forbidden.
2. The split valve: a spec may split internally at ITS plan time (e.g., 026a/026b) with scope conserved and no renumbering of others; the split is recorded in its plan.
3. Each spec owns exactly its page family; byte-identity outside it; one sanctioned smoke amendment per spec (own branch only, prior asserts byte-verbatim).
4. Acceptance floors in the sequence table are minimums — specs may exceed, never undercut.
5. Spec 027's eight machine-checkable rules are frozen now; 027 may add checks, never remove.

**Acceptance**: every future spec's plan opens by citing its sequence row; 027 publishes the close-out table against all floors.
