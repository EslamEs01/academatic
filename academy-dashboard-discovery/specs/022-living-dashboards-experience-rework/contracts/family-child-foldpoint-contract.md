# Contract — Family-Child Fold Point (DEC-006 · D7/D8)
family-child gains EXACTLY ONE addition: the preview panel in the intro area — copy prt.lv.child.*
(«افتح عرض الابن الكامل — معاينة توضيحية (سلمان)») + ONE real link → student-portal(.en).html.
The 5 baked panels, :target machinery, default-st1, 5 switcher links, zero-pay line: BYTE-KEPT
asserts. This DECLARES the supersession of the 020 body byte-equality baseline: the new extraction
hash is recorded as the 022 baseline; any delta beyond the one panel = STOP.
family-children: NO TOUCH (per-child links rejected — the preview persona is st1 only; links under
other children's names would lie). Its build output must be byte-identical; a rebake = STOP.
