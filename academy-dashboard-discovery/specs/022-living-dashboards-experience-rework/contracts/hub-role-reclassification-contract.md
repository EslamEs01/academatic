# Contract — Hub Role Reclassification (DEC-004 · D3/D4)
portals.js: the primary grid renders EXACTLY two role cards [family, teacher] (Arabic-first order);
the admin console band stays; a NEW demoted child-view entry renders BELOW the grid as a compact
card: tKey prt.lv.hub.cvT «عرض الابن — معاينة», copy naming the family journey AND سلمان, ONE real
link → student-portal(.en).html. The hub .pt-hero is upgraded in place (headline names the three
primary roles). NO fourth equal card · NO auth/login claim · NO deletion of any student link ·
demo notes kept. Smoke: hubRoleTargets set-equality {family-portal, teacher-portal} + hubAdminLink
+ EXACTLY 1 child-view link (own probe) + zero student-role primary card.
