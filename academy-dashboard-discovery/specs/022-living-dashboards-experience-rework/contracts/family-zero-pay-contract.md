# Contract — Family Zero-Pay (D16)
The verbatim payFigure regex (ريال|ر.س|SAR|USD|جنيه|EGP|[$€£]|ادفع|سداد|pay now|payment|amount|
price|مبلغ|سعر|رسوم) stays green on EVERY family body — the rebaked family-portal + family-child
AND all seven untouched internals (per-page smoke pins byte-kept). The family-home billing story
copy is authored against the regex BEFORE build. The original regex lines in smoke stay
BYTE-VERBATIM. Family hero counters are counts (children/sessions/attention) only.
