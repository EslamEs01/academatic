# Contract — Mobile / A11y / Motion (D13/D18)
Mobile 390: hero compresses (stacked counters), rail goes vertical, stories one-line, no horizontal
overflow (the 390 probe extends to all rebaked pages). A11y rows added: portals, family-portal,
teacher-portal, student-portal, family-child × (AR light + AR dark) + family-portal EN — axe
critical=0 serious=0 (hero gradient contrast is the known dark risk). Reduced-motion: the CSS audit
asserts every lv- animation/pulse reference sits inside the no-preference media query; with reduce
set, pages are complete and static. Focus rings preserved on all new link affordances.
