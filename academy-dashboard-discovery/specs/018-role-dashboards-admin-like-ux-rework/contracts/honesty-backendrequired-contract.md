# Contract: Honesty & backendRequired (Spec 018 scope)

**Status**: Binding · The 016 honesty law applied to this rework.

1. Four action classes only. Child links = class 1 (real). Quick-link planned tiles + section drill-down affordances for unbuilt targets = class 3 (labeled planned buttons). Everything else display-only.
2. Gates that move with displaced content keep their labeled availability vocabulary at their new (compact or future-page) locations; the home planned-tone counts are re-pinned in the smoke amendment.
3. Zero fake: no join/save/upload/submit/pay affordances anywhere in the rework; billing stays STATUS-only; requests stay teaser+gate.
4. Zero href="#"; zero dead links; truthful empties only (a child with no today session shows the honest none-state).

**Acceptance**: action-class sweep during review; smoke planned/anchor asserts; zero-pay + payHit byte-verbatim.
