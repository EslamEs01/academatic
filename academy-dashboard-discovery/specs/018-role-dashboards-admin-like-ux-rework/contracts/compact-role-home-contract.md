# Contract: Compact Role Home (Spec 018)

**Status**: Binding · The user's verdict, machine-enforced. References FR-001/002/003/005; research D1/D2/D3.

1. Each role home renders EXACTLY the 7-band recipe in order: compact header · 4-KPI row · now band (today ≤3 + next) · role-core · ONE preview · quick-links tiles · one-line note.
2. Ceilings, smoke-asserted at 1366×768: sections 4–7 · scrollHeight ≤ 2,200px (floor ≥ 900). The constant may retune ±10% within the 1–2-screen principle, recorded in REVIEW.md.
3. KPI figures = authored fixture literals via num(); labeled; never computed; never money-adjacent.
4. Role cores: student = homework snapshot (top-2 + gate) · family = children cards w/ the 5 REAL drill-down links · teacher = the follow-up board (real outcomes).
5. Quick-link tiles mirror the role nav minus home; planned targets use the honest button treatment (017 vocabulary); implemented targets become links only when their pages exist.
6. The long-portal style may never return: the ceiling assert is permanent for role homes.

**Acceptance**: all six built home files pass the window+ceiling probes; band order verified visually per role; REVIEW records heights before/after.
