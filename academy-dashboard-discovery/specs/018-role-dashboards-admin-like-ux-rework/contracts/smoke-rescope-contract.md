# Contract: Smoke Re-Scope (Spec 018 — the ONE sanctioned amendment)

**Status**: Binding · References research D7/D8. Reviewed as ONE diff at the G-audit.

REPLACED (the sanctioned re-scope of the long-home branches): student branch gauges≥2/sections≥10/empty≥1 → kpi===4 + sections 4–7 + ceiling probe (bodyAnchors===0 KEPT) · family branch bars===5/sections≥10/empty≥1/bodyAnchors===0/planned 2+2 → kpi===4 + window + ceiling + bodyAnchors===5 exact child targets + re-pinned gate tones (zero-pay regex line BYTE-VERBATIM) · teacher branch sections≥10/empty≥1/avatars≥6/planned 3+1 → kpi===4 + window + ceiling + re-pinned tones (bodyAnchors===1 + exact target + payHit lines BYTE-VERBATIM).
ADDED: the family-child branch (PAGES+PORTAL_PAGES entry → 50 loads; 5 panels, 5 tab triggers, default st1 visible, shell registry {family-portal,portals}×5, body 0, formControls 0, zero-pay, tables 0, 390 probe) + the 1366×768 ceiling/floor probe step for the three homes.
UNTOUCHED BYTE-VERBATIM: payHit block, the family zero-pay regex line, ALL Shell-v2 asserts, hub asserts, admin-scoped asserts, raw-key/dead-link/crawl machinery.

**Acceptance**: diff confined to the enumerated replacements/additions; all 50 loads green; the byte-verbatim lines show as context-only in the diff.
