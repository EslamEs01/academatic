# Contract: Displacement Map (Spec 018)

**Status**: Binding · References FR-006, US9; research D11. The spec's displacement table IS the law.

1. Zero capability deletion: every pre-018 home section lands in {kept-compact, family-child, Spec 019, Spec 020, Spec 021, honest gate}.
2. Displaced fixtures/locale keys are RETAINED in the repo (grep-verified at the G-audit); only the owning future spec may retire what it consciously replaces.
3. Specs 019–021 MUST consume the table as their content source (their pages re-render the displaced sections at full depth).
4. The homes may not re-absorb displaced detail (the ceiling forbids it) — deepening happens on internal pages only.

**Acceptance**: the table's rows all check off during the rewrites; the retained-keys grep is green; 019–021 briefs cite this contract.
