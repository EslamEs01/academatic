# Contract: Home Content Integrity (Spec 017)

**Status**: Binding · References FR-008, SC-003; research D8.

1. The three home pages keep their COMPLETE pre-017 content: every section, order, data, gate, and chip — deepening frames them, nothing edits them.
2. Proof, two layers: (a) `git diff` on the three page modules is empty (or a wrapper-argument-only line, enumerated in the report); (b) the extracted `#page-body` inner HTML of HEAD's six built role files equals the new build's — byte-compare.
3. All standing body asserts (student gauges/planned tones/bodyAnchors 0 · family bars/zero-pay/anchors 0 · teacher sections/empty/anchors 1+target/payHit/avatars) stay BYTE-VERBATIM in the smoke file and green.

**Acceptance**: both proof layers recorded in the G-audit; sibling asserts untouched.
