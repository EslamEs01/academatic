# Contract: Teacher Pay-Free (Spec 017 scope of the GLOBAL rule)

**Status**: Binding FOREVER · Implements the 016 teacher-pay-free-global contract for the shell layer.

1. The teacher registry entries, `prt.nav.tch.*` labels, icons, comments, and the deepened built teacher pair carry ZERO pay vocabulary — the EXTENDED set: word-bounded EN salary|salaries|pay|payouts?|earnings?|compensation|bonus|fines?|money|currency + AR راتب|رواتب|أجر|أتعاب|مستحقات|مكافأة|غرامة|فلوس + currency tokens EGP|SAR|USD|ريال|ر.س|جنيه|دولار|$|€|£.
2. Three layers re-run at completion: source grep (incl. comments) over teacher-owned + shell + registry surfaces · built grep over both teacher files · the BYTE-VERBATIM Spec-012 payHit smoke assert.
3. No teacher surface routes toward any pay surface; the sanctioned-anchor registry enforces it structurally (self + hub + the one performance body link only).

**Acceptance**: all three layers zero-hit; payHit green unchanged.
