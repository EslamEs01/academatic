# Contract — Teacher Portal Pay-Free (GLOBAL)

**Guarantee**: the 16 `teacher-*` portal files stay byte-identical; teacher pay-free law holds.
- 030 touches no teacher-portal file; no teacher-portal salary/pay page.
- `teacher-performance.html` stays display-only (no score/rank/chart/payroll); byte-identical.
- The 030 ADMIN salary boards are figure-free (sanctioned admin surface); `payHit`/`tchPay` byte-verbatim.
**Verify**: `git diff --stat` teacher-portal + teacher-performance empty; payHit/tchPay unchanged/green.
**Fail if**: a teacher-portal file changes; a teacher pay figure appears.
