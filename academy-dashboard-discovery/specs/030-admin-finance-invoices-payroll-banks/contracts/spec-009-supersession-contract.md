# Contract — Spec-009 Finance Invariant Supersession (DECLARED)

**Guarantee**: 030 is the sanctioned first modifier of finance.html; it SUPERSEDES the Spec-009 freeze via a declared amendment while keeping every permanent guarantee.
**LIFTED (declared)**: the "zero git diff" freeze on `pages/finance.js`/`fixtures/finance.js`/`components/finance-{status,actions}.js`/`locales/*.fin.js`; the `finance.html` `#page-body` byte-identical clause; the exact "9 planned cards" shape.
**KEPT permanent (byte-verbatim)**: no money arithmetic; no status mutation on confirm; no receipt/`type=file`; no chart/`<canvas>`; `FINANCE_SUMMARY` row-count-only; invoice/payment amount literals only (no aggregate); **salary/payroll/payout figures never shown**; the finance `forbidden` regex (chart|canvas|graph|score|rank|leaderboard|percentile); no finance-token leak on dashboard/reports bodies; six-wallet-locked nav + finance sub-section membership.
**Smoke amendment**: supersede the 9-planned-cards count + finance body structure; KEEP byte-verbatim the 4-tiles / 9-invoice-rows / 6-payment-rows / 9-drawers (Overview) + all permanent asserts. Reference `specs/011-…/contracts/finance-impact-contract.md:7` explicitly.
**Fail if**: a permanent guarantee is broken; the supersession is silent (not declared in the impl record).
