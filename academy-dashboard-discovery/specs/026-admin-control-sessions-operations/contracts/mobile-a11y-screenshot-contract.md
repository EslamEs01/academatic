# Contract — Mobile / A11y / Screenshots

**MUST**: a11y critical=0 serious=0 across new/changed ops pages + at least one modal + one drawer; screenshots capture the new surfaces.

**Acceptance**
- A11y matrix adds `sessions-analysis`/`public-holiday`/`scheduled-actions` (AR light+dark, EN sample) + one modal-open + one drawer-open state; keyboard/focus safe; gate aria labels; mobile-390; dark/light.
- Screenshots: 3 new pages AR desktop + ≥1 EN + a create modal + a details drawer + one action-gate + dashboard filter proof (if Option A) + mobile-390 + dark; `REVIEW.md` updated with a Spec 026 section.
- **Fail** if a11y critical/serious > 0 or mobile-390 overflows.
