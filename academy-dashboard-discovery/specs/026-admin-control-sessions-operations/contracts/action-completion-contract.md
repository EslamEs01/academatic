# Contract — Action Completion (the main-user-law)

**MUST**: every visible button/link/action across all 97 pages resolves to exactly one honest outcome — real page · modal · drawer · real static tab/filter/switcher · honest `backendRequired`/`planned`/`permission` gate · or removed/reworded. No inert or misleading UI.

**Acceptance**
- Every action classified (`current-action-inventory.md`) into the approved taxonomy; **0** forbidden classes.
- No «preview action» toast remains on any Create/Edit/Delete/Save/Print/Export/Upload action.
- `href="#"` = 0 sitewide; every `[data-action]` handled or resolves to an honest gate; no dead buttons (smoke).
- Every Create/Edit/Delete/View opens a page/modal/drawer/gate (smoke asserts per reclassified action).
- **Fail** if any action does nothing, fakes persistence, or reads as a working control it isn't.
