# Page vs Deep-link vs Fold — Decision Register (Spec 035)

For each of the four scoped Families/Students items, the resolved disposition. Confirms and locks the Spec 033 recommendations.

| Item | Decision | Reason | Evidence | Expected route / deep-link | Count | Acceptance check |
|---|---|---|---|---|---|---|
| **familyCategories** / فئات العائلات | **Folded owner (anchor)** | The category surface (browse-by-category filter + `fam-cat` display-only reclassify drawer with a gated Save) already exists on `families.html`; a standalone page would need forbidden category CRUD persistence. | `family.js:149-165` (`fam-cat` drawer), `families.js:32` (category filter), `families.js:39` (drawer baked here); Spec 027 M-K; Spec 032 FC-05; Spec 033 CS-06 / matrix row 30. | `families.html` (`.en` → `families.en.html`) | **0** | Nav item is a real anchor → families.html; 0 «قريبًا»; `fam-cat` drawer still reachable + Save still gated. |
| **scheduleSearch** / بحث الجدول | **Standalone page** | No real availability-search surface exists (Spec 027 M-S kept it a gate-only preview); `schedule.html` is a *browse* surface (filter over List/Timetable), semantically distinct from "search for an available slot/teacher." Spec 033 recommended standalone. | `schedule.js:45-77` (browse, not search); Spec 027 M-S ("availability-matcher preview; async search = backendRequired gate"); Spec 033 CS-07 / matrix row 32 / page-count-envelope (schedule-search named as a new pair). | `schedule-search.html` + `schedule-search.en.html` | **+2** | Page renders search form + results board + empty state; Book/Assign final = backendRequired gate; AR/EN both load. |
| **studentResult** / نتائج الطلاب | **Deep-link** | The display-only Results tab already exists on `student.html` and the tabs widget honors `#view=` on load; an aggregate results page has no honest content under the no-computed-score law. | `student.js:233,242` (results tab → `resultSummary`); `result-summary.js` ("NO gradebook … NO computed score"); `tabs.js:4`+`enhance.js:265` (`#view=` wins); Spec 033 CS-08 / matrix row 33; Spec 027 M-R. | `student.html#view=results` | **0** | Nav item anchors to `student.html#view=results`; the Results tab opens; no computed score/rank/chart introduced. |
| **studentEvaluation** / تقييم الطلاب | **Deep-link** | The display-only Evaluation rubric tab already exists on `student.html`; same `#view=` mechanism; aggregate evaluation page would invent rubric totals. | `student.js:234,243` (evaluation tab → `evaluationRubric`); `evaluation-rubric.js` ("NO scoring engine … NO persistence"); Spec 033 CS-09 / matrix row 34; Spec 027 M-R; Spec 029 R-F/US3 (real deep-link to the evaluation tab). | `student.html#view=evaluation` | **0** | Nav item anchors to `student.html#view=evaluation`; the Evaluation tab opens; no computed score/total introduced. |

**Net:** 1 standalone page pair (scheduleSearch, +2) · 1 fold-anchor (familyCategories, 0) · 2 deep-links (studentResult, studentEvaluation, 0) → **113 → 115**.
