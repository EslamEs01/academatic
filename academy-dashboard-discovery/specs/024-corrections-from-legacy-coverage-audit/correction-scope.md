# Correction Scope — Spec 024 Corrections From Legacy Coverage Audit

**Date**: 2026-07-07
**Baseline**: branch `feature/012-role-portal-foundation`, HEAD `837b0c1`; Spec 023 uncommitted working-tree baseline; 77 public HTML.
**Source of truth**: `specs/023-full-legacy-coverage-audit/correction-backlog-for-024.md` (B-01…B-18) + registers. This file translates the Must/Should-Fix backlog into a per-item implementation scope for the later `/speckit-plan` + `/speckit-implement` phases. It defines WHAT changes where and the exact acceptance check — it does NOT change code.

## Priority buckets (from the Spec 023 backlog)

- **Must fix in 024**: B-01, B-02, B-03, B-04
- **Should fix in 024**: B-05, B-06, B-07, B-08, B-09 (finance-boundary; B-09≡B-15 note), B-10, B-11
- **Later / Do-not-fix (NOT implemented here)**: B-12 (scanner hygiene), B-13 (FUTURE_ROUTES map), B-14 (gallery nav check → 032), B-16 (settingsUsers merge → 031), B-17 (family-children no-fold-link — recorded as protected in B-10's determination), B-18 (exclusion register → folded into B-08). Any of these MAY be picked up opportunistically only if it is a zero-risk one-line record; otherwise deferred with the reason noted.

## Per-item scope

| B-ID | Priority | Correction | Files touched | Change type | Acceptance check | Law guards |
|---|---|---|---|---|---|---|
| **B-01** | Must | Reframe the child-view note `noteT`/`noteD` (ar+en) from «لوحة الطالب — النسخة الأولى» / "Student dashboard — first version" to child-view / family-owned wording aligned to the existing «عرض الابن» vocabulary (e.g. ar `noteT: 'عرض الابن — النسخة الأولى'`, guardian-addressed `noteD`; en "Child view — part of the family account"). Rebake the 6 affected pages; re-record the affected `#page-body` extraction hashes as a declared baseline change; re-pin smoke. | `app/src/locales/ar.prt.js:297-298`, `en.prt.js:294`; built `student-{portal,homework,history,profile,progress,materials}.html`(+`.en`) + `app/public/assets/locales/ar.prt.js`; `app/tests/smoke/run.cjs` (hash pins) | copy + rebake + hash supersession | `grep -RIl "لوحة الطالب" app/public/student-*.html` → 0; `grep "student dashboard" app/public/student-*.en.html` → 0; family note (`:387-388`) + teacher note (`:446-447`) byte-unchanged; 6 hashes re-recorded; smoke green | student demoted-not-deleted; no fake; byte-freeze respected via declared supersession |
| **B-02** | Must | Record the ownership DECISION for the admin RBAC Locations capability: owner **Spec 031** (a display slice inside settings/general — legacy general settings already carries Country/City/Timezone/Address). No page built. If a natural current surface exists, an honest planned/backendRequired note MAY be added; otherwise owner is documented only. | Correction/coverage records (this spec + append-only note to `specs/023-…/coverage-matrix.md` §Locations if desired); optionally `nav.config.js` (honest planned note only) | decision record (+ optional honest note) | A written owner (031) exists; the 57-row inventory reads as 58-owned; 032 no-missing audit would pass; no fake Locations page | no new page; honest gate only |
| **B-03** | Must | Resolve notifications on the **role-portal** surfaces (family/teacher/student): reuse the EXISTING honest pattern (`enhance.js notificationsMenu()` — Soon-badged, `aria-disabled`, `data-disabled-reason`; no new hook) as a role-shell gate, OR record a futures-register entry. No fake count/read/unread/popover beyond the existing pattern. Admin gate untouched. | Decision record; if UI added: `app/src/js/components/portal-shell.js` (reuse `data-action="notifications"`) + `ar/en` `topbar.notif*` keys (already exist) + `app/tests/{smoke,a11y}` coverage | decision (+ optional honest gate reusing existing hook) | Notifications appear only as an honest gate (Soon/backendRequired) OR a futures record; zero fake badge/count/read state; admin `notificationsMenu()` byte-unchanged; a11y green if UI added | no fake engine; no new hook; honest gate |
| **B-04** | Must | Record the teacher live-room as **future-backend** (no capture exists in the evidence tree). Teacher surfaces show any live/room action only as an honest gate. No fake enter-class/end-class/attendance-write. | Decision record (this spec + Spec 023 status note); no app change | decision record | Live-room recorded future-backend (or fresh capture cited); coverage-matrix teacher row moves off `unclear-needs-review`; no fake live behavior in teacher pages | no fake action; no real live room |
| **B-05** | Should | Give teacher library an honest planned presence: EITHER add a `library` «قريبًا» item to `ROLE_NAV.teacher` (renders as a non-anchor button, `plannedNavAnchors===0` holds) OR record a decision to fold materials into an owned Spec 025 page. Owner **Spec 025**. No internal page built. | `app/src/js/fixtures/portal.js` (`ROLE_NAV.teacher`) if a nav item is chosen; `ar/en` `prt.nav.tch.library` key if added; else decision record | decision (+ optional 1 planned nav item) | Teacher library has an honest «قريبًا» presence OR a recorded 025 fold decision; if a nav item is added it is a non-anchor button; smoke `plannedNavAnchors===0` still green | honest gate; no fake page; no new hook |
| **B-06** | Should | Classify teacher chat backendRequired/future with a recorded owner: honest «قريبًا» item in 025 vs explicit teacher-side exclusion (admin preview stays 026). Any chat mention = honest gate; send-form UNCONFIRMED (never invent fields). | Decision record; optionally `ROLE_NAV.teacher` (honest «قريبًا» chat item) + `ar/en` key | decision (+ optional honest gate) | Chat is classified backendRequired/future with a written owner; no fake chat send; if surfaced, honest non-anchor gate | no fake chat; honest gate |
| **B-07** | Should | Record a written pay-free-contract exemption for the pre-existing **Spec 007** admin `teacher-performance` board (body pay-free + smoke-asserted `run.cjs:548-561`; the admin-shell nav chrome is the reason) and schedule the Spec 025 repoint of the `teacher-portal.html:378` → `teacher-performance.html` anchor to the real teacher-reports internal. | `specs/016-…/contracts/teacher-pay-free-global-contract.md` (+ `specs/022-…/contracts/teacher-pay-free-contract.md`) — exemption note; Spec-024 record of the 025 repoint task | documentation record | The pay-free contract carries the written exemption citing Spec 007 provenance; a 025 repoint task is recorded; teacher-portal pay-token scan stays zero-hit | teacher pay-free GLOBAL binding |
| **B-08** | Should | Add durable provenance records for the intentional exclusions: teacher pay surfaces, family Amount/payment, chart/score engines, fake-action engines, finance/payroll UI boundary, notifications/shortcuts. Each names its governing law. | Correction/coverage docs (this spec + append-only Spec 023 exclusion notes); README/CLAUDE where apt | documentation record | Each exclusion appears in a record with its law cited; 032 no-missing audit would not flag them | excluded-by-law preserved |
| **B-09** | Should | State the finance boundary in one sentence in the correction record (and the 030 contract when it lands): authored admin invoice-amount literals are Spec-009-sanctioned (zero aggregate/math, admin-only); salary/payroll/compensation figures are NEVER allowed anywhere; family/teacher stay figure-free. | Correction record (this spec); boundary note carried toward the 030 contract | documentation record | The one-sentence boundary exists; no app change; `finance.html` invoice literals unchanged; family/teacher figure-free | zero-pay; Spec 009-invariant |
| **B-10** | Should | Verify against the Spec 022 contracts that the living rework moved (not deleted) the pre-022 rail prep-hint/count content; prove moved content reachable; restore as a real link/honest gate only if genuinely deleted. Record the determination (also carries the B-17 family-children-no-fold-link protection note: intentional, do NOT add per-child links). | `specs/022-…/contracts/{teacher-living-home-contract.md, impact-protection-contract.md, smoke-rescope-contract.md, visual-regression-screenshot-contract.md}`; `app/screenshots/before-022/*`; if restore needed: `app.css`/rail content (feeds B-11) | verification + determination record (+ conditional restore) | A written moved-vs-deleted determination exists; no prior important entry point left deleted; family-children no-fold-link recorded intentional | no deletion of useful work; no fake per-child link |
| **B-11** | Should | One pure-CSS visual-density pass on the additive living layer addressing (scoped): D-01 empty rails (second content column / capped width), D-04 family-children storyRow + «اليوم:» tag, D-05 compact stat tiles, D-06 role-tinted dark hero, D-07 schedule hierarchy, D-08 hub 2-up row, D-09 7-cell week strip, D-10/D-11/D-12 delight touches, D-13 mobile topbar. Rows touching pinned bodies (D-04/D-05/D-09) carry a declared hash supersession. | `app/src/styles/app.css` (living layer); pinned `#page-body` where D-04/D-05/D-09 require it; `ar/en` empty-state copy; `app/tests/smoke` re-pin where hashes change; `app/screenshots/REVIEW.md` | pure-CSS + minor copy (+ hash supersession where needed) | Each scoped D-row shows a before/after improvement; motion inside the one reduced-motion block; zero new hooks/pages/engines; smoke/a11y/screenshots green | reduced-motion; no engine; no new hook; static-safe |

## Deferred items (explicitly NOT implemented in 024)

| B-ID | Reason deferred | Owner |
|---|---|---|
| B-12 (scanner hygiene: EN "pay" comment, family "fine 🌿") | Can-schedule-later; no rendered-output effect | later / 024-opportunistic |
| B-13 (FUTURE_ROUTES doc map pre-fill) | Cosmetic documentation; each 026–031 extends it | later |
| B-14 (gallery nav-exposure machine check) | Owned by 032-final-qa | 032-final-qa |
| B-16 (settingsUsers duplicate merge) | Owned by 031 (decide single staff home) | 031 |
| B-17 (family-children no-fold-link) | Protective record only — folded into B-10's determination (do NOT "fix") | recorded in B-10 |
| B-18 (broad exclusion register) | Folded into B-08 provenance record | recorded in B-08 |

## Global guards for every item

- Public HTML count stays exactly **77**; no new page.
- No new `data-*` hook, no new storage key, no `href="#"`, no raw locale key.
- Teacher pay-free GLOBAL + family zero-pay token scans stay green.
- Any `#page-body` change ships a declared hash supersession + one sanctioned smoke amendment — never a bypass.
- All motion stays inside the single `prefers-reduced-motion: no-preference` block.
- Student demoted-not-deleted; family owns the child journey; admin/family/teacher primary — unchanged.
- Dark/light, RTL/LTR, mobile-390 all preserved.

## Continuation

After Spec 024 closes B-01…B-04 (Must) and B-05/B-06/B-07 (the Spec-025 prerequisites), **Spec 025 Teacher Internal Pages is GO** per the Spec 023 GO-conditional gate. B-08…B-11 are Should-Fix quality/documentation and do not block 025.
