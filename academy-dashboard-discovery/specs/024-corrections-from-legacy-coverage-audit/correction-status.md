# Correction Status — Spec 024 implementation record

**Date**: 2026-07-07
**Baseline**: branch `feature/012-role-portal-foundation`, HEAD `837b0c1` (Spec 023/024 uncommitted working-tree baseline); 77 public HTML held.
**Scope**: implements the Spec 023 backlog B-01…B-11 per `plan.md`/`correction-scope.md`. This is the durable per-item implementation + decision record.

## Implemented (code)

- **B-01 — child-view wording (Must)** — DONE. Reframed the student child-view note in `app/src/locales/ar.prt.js:297-298` («عرض الابن — النسخة الأولى» + guardian-addressed `noteD`) and `en.prt.js:294` ("Child view — part of the family account"). Rebaked; 6 child-view pages + `.en` pairs updated; `assets/locales` regenerated. Grep gates green: zero «لوحة الطالب»/«بوابة الطالب» in `student-*.html`, zero "student dashboard"/"Student Portal" in `student-*.en.html`. Family note (`ar.prt.js:387-388` «لوحة العائلة») and teacher note (`:446-447` «لوحة المعلم») BYTE-UNCHANGED (both primary roles — correct). **Hash supersession**: see `specs/022-…/contracts/student-preservation-contract.md` append below. Smoke guard added (`run.cjs`): child-view `#page-body` must not match `/لوحة الطالب|بوابة الطالب|student dashboard/i`.
- **B-03 — role-portal notifications honest gate (Must)** — DONE, **Option A**. Added a `data-action="notifications"` bell `icon-btn` to the role topbar in `app/src/js/components/portal-shell.js` (family/teacher/student), reusing the EXISTING `enhance.js notificationsMenu()` popover (Soon-badged, `aria-disabled`, `data-disabled-reason`). NO new hook, NO new storage key, NO fake count (the `<span class="dot">` unread indicator was deliberately omitted). Admin `notificationsMenu()`/`topbar.js` byte-unchanged. The control is a `<button>` (non-anchor) → shell-anchor multiset (5) unchanged; smoke green; a11y clean.
- **B-05 — teacher library planned nav item (Should)** — DONE, **Option A**. Added `{ id: 'library', labelKey: 'prt.nav.tch.library', icon: 'book-open', page: 'teacher-library', status: 'planned' }` to `ROLE_NAV.teacher` (`app/src/js/fixtures/portal.js`), owner Spec 025. Mirrored labels `nav.tch.library` = «مكتبتي»/"Library" (`ar/en.prt.js`). Renders as a non-anchor `is-planned` «قريبًا» button (verified: `<button>`, no href). Smoke re-pinned: `run.cjs` teacher nav count 7 → 8; `plannedNavAnchors===0` holds.
- **B-11 — visual-density pass (Should)** — DONE (CSS-only subset). Additive living-layer rules in `app/src/styles/app.css` (before the motion block): **D-06** dark-mode role-tinted idHero (theme-aware: `[data-theme="dark"]` + system-dark, mixes `--pt-accent` so family/child heroes keep their role color in dark), **D-08** hub primary grid 2-up at ≥720px (fills the empty third slot after the Spec-021 student-card demotion), **D-13** hide the redundant `.pt-greet` below 560px so the topbar action row (now with the notifications control) never wraps awkwardly. All pure CSS; motion untouched (stays in the single reduced-motion block); no new hook/page.
  - **Deferred (pinned-body rows)**: D-04 (family-children storyRow), D-05 (compact stat tiles), D-09 (week strip) — DEFERRED to a later design spec: each needs `#page-body` markup, and a correction spec should not churn hash-pinned bodies for pure polish. D-02 = F-00-1 (handled by B-01). D-14 admin cards → 026–031; D-15 data sheets stay light.

## Decisions / provenance records (no code)

- **B-02 — Locations owner** — Owner = **Spec 031** (admin management/settings → General configuration display slice; legacy general settings already carries Country/City/Timezone/Address — `output/roles/admin/screenshots/management-settings-general-full.png`). NO page, NO nav item, NO fake Locations UI built in 024. This record + the 023 coverage map are the owner decision. The admin capability map now reads as fully owned (Locations → 031); Spec 032's no-missing audit will not flag it.
- **B-04 — Teacher live-room** — Recorded **future-backend**. No live-session capture exists in the evidence tree (`output/roles/teacher/pages/teacher-session-class-room-mq-2.md` is a redirected `/teacher/home` copy). Teacher surfaces show any live/room action only as honest gates; NO fake enter-class/end-class/attendance-write. The coverage-matrix teacher `session-class-room` row is hereby moved off `unclear-needs-review` → `intentionally-excluded / future-backend`.
- **B-06 — Teacher chat** — Recorded: teacher chat = **backendRequired / future**; NO 024 nav item (no retained teacher-side fixture; the send-form was never captured — UNCONFIRMED). Owner decision deferred to **Spec 025** scoping (may add an honest «قريبًا» chat item if the teacher IA warrants it, else it stays the admin-side preview owned by 026). NO fake chat send, NO invented send-form fields.
- **B-07 — Pay-free exemption** — Recorded in `specs/016-…/contracts/teacher-pay-free-global-contract.md` (append below). `teacher-performance.html` is a pre-existing **Spec 007 ADMIN** board (admin `app-shell`, `activeId teacherKpi`); its `#page-body` is pay-free and smoke-asserted (`run.cjs:548-561`); the الرواتب tokens are admin-shell nav chrome, not teacher-owned content. The sanctioned teacher-home→performance anchor is grandfathered pending the **Spec 025** repoint to the real `teacher-reports` internal (+ demote the admin-board link to admin-only). The pay-free grep is NOT weakened.
- **B-08 — Exclusion provenance** — The intentional exclusions (teacher pay surfaces, family Amount/payment, chart/score engines, fake-action engines, finance/payroll UI boundary, notifications/shortcuts personalization) are recorded here + in README/CLAUDE, each under its governing law (teacher pay-free GLOBAL, family zero-pay, no computed-score/chart engine, no-fake-actions, static-fixtures-only).
- **B-09 — Finance boundary** — Recorded in `app/README.md` + CLAUDE.md: authored admin invoice-amount literals on admin finance pages are Spec-009-sanctioned (zero aggregate, zero runtime math, admin-only); salary/payroll/compensation/payout **figures** are NEVER allowed anywhere; family and teacher surfaces stay figure-free.
- **B-10 — Rail moved-vs-deleted** — Determination: **MOVED, not deleted.** The pre-022 rail «التحضير المقترح» prep-hint (`app/screenshots/before-022/teacher-portal__ar__light__desktop.png`) moved into the dedicated `pt-flow` prepare→attend→record→review strip — the «التحضير» step carries «راجع الدرس وجهّز المواد.» (`ar.prt.js` `tea.flow.prepT/prepD`, rendered in `public/teacher-portal.html`). Room («قاعة أ/ج») + per-session student counts survive as rail chips. No prior important entry point was deleted; the no-deletion law holds. No restore required. **B-17 companion**: `family-children` carries NO fold-point link (5 drill-down anchors, zero `student-portal`) — this is an INTENTIONAL, honesty-driven deviation (per-child child-view links rejected as dishonest; preview persona is st1/سلمان only). Do NOT "fix" it.

## Deferred (NOT implemented in 024)

B-12 (scanner hygiene), B-13 (FUTURE_ROUTES map), B-14 (gallery nav-check → 032), B-16 (settingsUsers merge → 031). B-17 recorded inside B-10; B-18 recorded inside B-08.

## Verification (see the final report for full audit)

77 HTML held · B-01 grep gates 0/0/0/0 · family/teacher notes unchanged · teacher pay-free (precise word-bounded scan) 0 hits · no `href="#"` · library + notifications are non-anchors · smoke green (76 loads) · GO for Spec 025 after 024 (B-04/B-05/B-06/B-07 closed).
