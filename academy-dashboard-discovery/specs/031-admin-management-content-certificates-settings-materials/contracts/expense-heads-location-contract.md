# Contract: Expense-Heads & Locations

**Purpose**: Two no-page fold slices inside settings General — figure-free lookup + location display.

**MUST**:
- **Expense-heads**: name + status-chip lookup rows + Add/Edit name-status backendRequired modal + Delete gate. **NO amount / no figure** (owned by 031 per `030/future-owner-register.md`, superseding the earlier finance placement). Finance-adjacent but figure-free.
- **Locations**: a display slice (country/city/timezone/address) inside the General tab. **No dedicated Locations page** (B-02/M-04 — RBAC-group-name only, no crawled page).
- Neither mints a new page; both live in the settings hub.

**Verify**: smoke — General tab shows a heads lookup (no amount) + a locations slice; `figureFree`; count unchanged by these.

**Status**: Binding. `future-owner-register.md` boundary note; Register #16.
