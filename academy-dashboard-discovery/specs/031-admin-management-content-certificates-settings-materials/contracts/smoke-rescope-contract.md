# Contract: Smoke Rescope

**Purpose**: One sanctioned additive smoke amendment; no protected assert weakened.

**MUST**:
- Add an **additive** 031 honesty block modeled on the finance `f30` block (`run.cjs:1019-1056`) over the built 031 bodies (settings hub + staff + library + certificates, AR+EN).
- Assert: tabs render (settings 6 / library 2 / certificates 2), 1 panel visible each; staff directory + kebab + RBAC matrix; Add/Edit modal no `type=password`/no salary; library Materials+Books + category drawer + no `type=file`; certificates static designer (no `<canvas>`/drag) + Approve gate + no-mutation; integrations locked cards + `noSecret`; global `noFile`/`noPdf`/`noBackup`/`noCanvas`/`figureFree`/`FAKE`-clean/`href="#"`=0/raw-keys=0/dead-buttons=0; count=103; admin-menu coverage re-pin.
- Update the PAGES/loads count and add the 3 new pages to the smoke page set.
- **Byte-verbatim** (unchanged): `payHit`/`tchPay`/`famPay`/`payFigure`/child-view/finance-no-mutation/finance-`forbidden` + all 026/027/028/029/030 asserts.
- Any hit on a forbidden token = smoke FAIL (never weaken an assert to pass).

**Verify**: smoke diff = additive block + count/loads update + coverage re-pin; protected regexes not in the diff.

**Status**: Binding.
