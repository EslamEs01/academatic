# Contract: Teacher Portal Foundation (Spec 012)

**Status**: Binding · The sara foundation page — deep dashboard = Spec 015. References FR-002/FR-004/FR-005/**FR-006**; US4; research D3/D9.

## 1. Composition

1. **Teacher welcome** — sara, organized/respectful tone, today-first framing.
2. **Today's schedule preview** — sara's fixture sessions today (via teacher-links/schedule), time-ordered cards.
3. **Next live session preview** — course/group/time card whose affordance is honest demo/planned and explicitly NOT a real call join (the legacy "live room" was itself fake — classified excluded).
4. **My students preview** — sara's fixture students as friendly cards (name, subject, labeled follow-up signal — reusing existing labeled maps).
5. **Outcome workflow preview** — a display-only glimpse of the end-of-session recording flow (remark/summary concepts as labeled steps) — no working form, no write.
6. **Planned cards** — materials/library · tasks — labeled availability.
7. **Optional labeled admin link** — one real link to `teacher-performance.html` labeled as the admin view (plan default: include).
8. **Spec-015 note** — the deep teacher dashboard arrives in Spec 015.

## 2. The pay-free rule (hard, grep-enforced)

**Zero salary/pay/earnings/compensation figures or vocabulary anywhere on the page in both languages** (EN: `salary|salaries|payout|pay|earnings|compensation|fine|bonus` in pay sense · AR: راتب/رواتب/أجر/مستحقات/غرامة/مكافأة). The legacy salary hero/ledger/class-report are classification rows (backendRequired) only. Smoke asserts the built teacher-portal bodies token-free; the scope guard greps the sources.

**Acceptance (binding):**
1. **Given** the page, **When** sections are enumerated, **Then** all blocks render with sara's real fixture data and the join affordance is honestly non-real.
2. **Given** both built teacher-portal files, **When** greped for the pay-token sets, **Then** zero matches.
3. **Given** the whole page, **When** reviewed, **Then** it reads fast-daily-workflow (today first), card-based, mobile-comfortable — not the legacy KPI/salary home.
