# Contract — Scope Guard (Spec 028)
**Allowed to create/modify** (narrowed): `pages/{teachers,teacher,teacher-performance,course,group}.js`; `components/{teacher-actions,teacher-signals,teacher-status,course-group-actions,directory-card,table,data-table}.js`; `enhance.js` (add `teacherMenu` builder + a `'teacher'` row-menu dispatch branch mirroring `familyMenu`/`studentMenu` — reuses the existing `data-row-menu` hook; no new dispatch hook); `fixtures/teacher-management.js` (new) + `fixtures/{teachers,teacher-links}.js` (only if a candidate needs an existing non-pay field); `locales/{ar,en}.trn.js`; `styles/app.css` (additive); `tests/*`; `screenshots/REVIEW.md`; `README.md`; `CLAUDE.md`; the 028 spec dir; append-only 016/023/025/026/027 records. `schedule.js` = read-only reference (the teacher-lens already exists).
**Forbidden** (STOP + report): `package.json`; dependencies; backend/API/auth/database; teacher-portal chat/pay/finance/live-room page; family payment page; student primary-role page; admin finance/reports(non-teacher-perf)/settings pages; new CRUD/chart/notification/chat/salary-payroll engine; new `data-*` dispatch hook or storage key; **any new standalone page** (count stays 97 — all-teachers-timetable folds into schedule.html); building any 029–032 / future-backend domain; any salary/payroll/compensation/payout figure or Salary/Payout fieldset; surfacing the `rating` field; changing any of the 16 teacher-portal HTML files.
**Stop conditions**: count ≠ 97 at start or after build; a new page where a modal/drawer/fold suffices; any T-row unresolved or action unclassified; any fake save/delete/assign/status-change/schedule-mutation; computed score/rank/chart; surfaced `rating`; teacher pay figure / Salary-Payout fieldset / compensations surface; teacher-portal byte-diff or `teacher-*` pay token; family pay figure; student-primary wording; `href="#"`/dead button/raw key; new engine/hook/dependency; a Spec-026/027 protection regresses; a11y critical/serious.
**Excluded-by-law guards** (carry forward): teacher portal pay-free GLOBAL; salary/payroll/compensation/payout figures never; family zero-pay; student child-view; admin finance Spec-009 invariant; no computed score/rank/chart; no fake CRUD/assignment/schedule mutation.
