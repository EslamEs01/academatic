# Contract — Smoke Rescope (Spec 028)
**MUST**: extend `tests/smoke/run.cjs` additively (ONE sanctioned amendment). Keep `payHit`/`tchPay`/`famPay`/`payFigure`/child-view/admin-finance + the Spec-026/027 action-completion asserts **byte-verbatim**.
**Acceptance (new asserts)**
- Count stays **97**; teachers/teacher/teacher-performance load.
- `teachers.html` has a working card kebab (View real · Edit modal · On-Vacation/Deactivate/Delete confirm), each honest.
- course/group + teacher-detail assign-teacher open a display-only picker → backendRequired; edit/note open modals (not bare toasts); status/delete = confirm → backendRequired; availability drawer opens; category modal/drawer opens.
- teacher-performance carries **no computed score/rank/chart**; admin teacher surfaces carry **no pay figure** (excluding the sanctioned `teacher-performance.html`).
- `href="#"`=0; no dead buttons; no fake final; no «preview action»; the 16 teacher-portal files byte-identical.
- **One sanctioned amendment**; protected asserts unchanged.
- **Fail** if a protected assert is weakened or a new action is unasserted.
