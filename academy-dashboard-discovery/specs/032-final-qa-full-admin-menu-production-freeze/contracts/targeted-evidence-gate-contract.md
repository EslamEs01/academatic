# Contract: Targeted Evidence Gate

**Purpose**: Every rebuilt form's fields trace to legacy evidence — no invented fields.

**MUST**:
- Every `formDrawer` field list maps to a row in `create-edit-forms-completion-inventory.md` / `data-model.md` with a `form-inventory.md` citation (or a documented grounded substitution: Add-Note→entity notes field; create-group-from-course→prefilled group form; group-edit→create field set; teacher-feedback-category→family-category shape).
- No field is invented; where legacy evidence is absent, use a clearly-authored safe demo field and record it.
- Option-select values come from authored fixtures (no computed/pay/PII values).

**Verify**: cross-check `data-model.md` field specs against `create-edit-forms-completion-inventory.md` citations.

**Status**: grounding complete (6-agent audit). Binding.
