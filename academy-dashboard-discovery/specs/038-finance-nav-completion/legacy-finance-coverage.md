# Legacy Finance Coverage — Spec 038 (Finance Nav Completion)

Grounding sources: `academy-dashboard-discovery/output/combined/page-inventory.md` (grep: invoice,
payment, salary, bank, analysis, monthly, expense) + targeted reads of
`output/roles/admin/pages/management-analysis-expenses.md`,
`management-analysis-invoices.md`, `management-monthly-invoices.md`,
`management-salary-class-report.md`. Current-state grounding: `app/src/js/nav.config.js` (the
`cat.finance` sub-section, exactly 7 `disabled`+`lock` items), `app/src/js/pages/finance.js` +
`app/src/js/fixtures/finance.js` (Spec-030 tabbed hub: Overview / Salaries / Banks), and
`app/tests/smoke/run.cjs` (the `lockedFin` array line 1586, the finance-body asserts around
line 960–1040).

Baseline: HEAD `56bc418` (Specs 035/036/037 committed, tree clean). `finance.html` is the
Spec-030 tabbed hub: **Overview** tab (9 baked invoice rows + 6 baked payment rows +
`FINANCE_SUMMARY` row-count-only tiles + 9 `PLANNED_FINANCE` cards), **Salaries** tab (`SALARIES`
fixture, figure-free, role-filterable teacher/staff), **Banks** tab (`BANKS` fixture, name+status).
All 7 finance nav items (`invoices`, `monthlyInvoices`, `salaries`, `staffSalaries`, `payments`,
`classSalaryReport`, `banks`) currently render `status:'disabled'` + `reasonKey:'nav.reason.finance'`
(honest lock) in the `cat.finance` sub-section of `nav.config.js`, even though most of their
underlying content is already baked inside `finance.html`'s tabs.

| Legacy capability | Evidence path | Money-centric? | Current frontend state (finance.html) | Disposition (Spec 038) | Honest-money boundary | Acceptance check |
|---|---|---|---|---|---|---|
| **Invoices** (`management/invoices`, `management/downlaod` export variants) | `page-inventory.md:128-146`; legacy list = per-family invoice rows, date/status filters (Paid/Unpaid/SoftDelete), a "Create Parent Invoice" wizard, CSV/PDF download variants | Yes — per-row amount, but no aggregate math on the list itself | Already baked: Overview tab, 9 `INVOICES` rows (serial/family/course/month/amount SAR/status), 4 status tiles (row-count-only), 9 invoice drawer templates, Create/CSV/PDF/Print all `data-disabled-reason` gates | **Unlock**: nav `invoices` → `finance.html#view=invoices` (deep-link into the existing Overview invoice list/section, no new board) | Authored per-row amount literal OK; `FINANCE_SUMMARY` tiles stay row-count-only (no SAR sum); Create/CSV/PDF/Print/Record-payment stay `backendRequired` gates | `lockedFin` no longer includes `invoices`; `#view=invoices` opens Overview scrolled/focused on the invoice list; 9-row/9-drawer/4-tile asserts unchanged |
| **Monthly Invoices** (`management/monthly-invoices`) | `page-inventory.md:152`; `management-monthly-invoices.md` — date-range filter form, a `# / Parent / Status` table (legacy sample: "No data found"), Search submit | Yes (invoice-adjacent), but legacy capture shows an empty/near-empty grid — no computed total on the page itself | Represented today only as the `monthlyInvoices` entry inside the 9 `PLANNED_FINANCE` cards (`availability:'planned'`, figure-free, amber tone) — no dedicated view | **Unlock**: nav `monthlyInvoices` → `finance.html#view=monthly-invoices` (deep-link to the Overview section containing that planned card; the card itself stays a locked/planned affordance — no new board fabricated from a legacy "No data found" capture) | No amount/total field invented for a page whose only legacy evidence was an empty grid; stays figure-free | `lockedFin` drops `monthlyInvoices`; `#view=monthly-invoices` scrolls/focuses the existing planned card; `plannedN===9` held (card stays, only its nav gate is removed) |
| **Payments** (`management/accounting/transaction/*`, i.e. payment collection ledger) | `page-inventory.md:14-19`; Accounting Transaction Invoices/Salary/Session captures — payment/wallet ledger rows keyed by status (attend/absent) | Yes — per-row amount | Already baked: Overview tab, 6 `PAYMENTS` rows (invoiceId/family/date/amount SAR/method/status: recorded/pending/returned) | **Unlock**: nav `payments` → `finance.html#view=payments` (deep-link into the existing Overview payment list) | Authored per-row amount literal OK; no aggregate/sum surfaced; no gateway/reconcile function — those stay gates | `lockedFin` drops `payments`; `#view=payments` opens Overview scrolled/focused on the payment list; 6-row assert unchanged |
| **Salaries (teacher)** (`management/salaries`, `teacher/salary`) | `page-inventory.md:241,334`; `Teacher Salary` capture — wallet/finance module, teacher-facing pay page (excluded by the teacher pay-free law) | Yes — the legacy admin page is a payroll figure page | Already baked: Salaries tab, `SALARIES` fixture filtered `role==='teacher'` — name + status (approved/pending/paid/on-hold) + period, **no amount field** | **Unlock**: nav `salaries` → `finance.html#view=salaries` (deep-link to the Salaries tab, teacher rows) | FIGURE-FREE by law (Spec-030 invariant): no pay amount ever rendered; Generate/Approve/Mark-paid/Export stay gates | `lockedFin` drops `salaries`; `#view=salaries` opens the Salaries tab; salary board stays digit-free for amount fields |
| **Staff Salaries** (`management/staff-salaries`) | `page-inventory.md:242` (sidebar link only; capture not separately crawled — folded under the same Wallet/Finance module as Salaries) | Yes | Already baked: Salaries tab, `SALARIES` fixture filtered `role==='staff'` — name + status + period, no amount | **Unlock**: nav `staffSalaries` → `finance.html#view=salaries` (or a `#view=staff-salaries` sub-anchor selecting the staff role filter within the same tab — implementation detail for plan.md) | FIGURE-FREE (same invariant as Salaries) | `lockedFin` drops `staffSalaries`; deep-link opens the Salaries tab (staff rows visible, either by default sub-view or a role toggle) |
| **Salary Class Report** (`management/salary-class-report`, `teacher/salary-class-report`) | `page-inventory.md:182,335`; `management-salary-class-report.md` — a group-by (Student/Teacher) date-range report form; the captured grid was empty at crawl time, but the capability is explicitly a per-class **salary** breakdown (teacher pay ties session→pay) | Yes — legacy intent is a computed pay-per-class report | Represented today only as a `classSalaryReport` entry inside the 9 `PLANNED_FINANCE` cards (`availability:'backendRequired'`) | **Open** (per Spec 038 scope): either (a) fold into a NEW figure-free "board" tab listing session→teacher/date pairs with NO pay amount, or (b) keep it an honest locked deep-link (`finance.html#view=class-salary-report`) pointing at the existing planned card, unchanged. Decision deferred to `plan.md`; this register records both paths so neither is silently assumed | If (a): categorical rows only (date/teacher/student/session), zero SAR/amount column — a genuine class↔pay linkage cannot be shown without real payroll math; if (b): stays `backendRequired`, no board fabricated | `lockedFin` drops `classSalaryReport` either way; if (a) ships, a smoke assert proves the new board is digit-free for pay; if (b), `plannedN===9` holds unchanged |
| **Banks** (`management/banks`, `management/banks/create`) | `page-inventory.md:37-38`; `Banks List` / `Create Bank` — a bank name + status list, a create-bank form | Marginally (a payment-rail metadata list, not an amount page) | Already baked: Banks tab, `BANKS` fixture — name + status only, no balance; Add-bank/Import/Reconcile are gates | **Unlock**: nav `banks` → `finance.html#view=banks` (deep-link to the existing Banks tab) | Name+status only, forever — no balance/account-number/credential field | `lockedFin` drops `banks`; `#view=banks` opens the Banks tab; bank rows stay balance-free |
| **Analysis — Expenses / Profit & Losses** (`management/analysis-expenses`) | `management-analysis-expenses.md:38-58,124-148` — a monthly table + KPI cards computing **Expected/Actual Net Profit, Expected/Actual Revenue, Teachers Salaries, Staff Salaries, Expenses, Total Expenses** (all live EUR figures, e.g. "Staff Salaries EUR 9,333.00") | Yes — the single most money/aggregate-heavy legacy surface found: computed net profit, revenue, and salary totals in one grid | No nav item exists today (not in the 7 `lockedFin`, not a `PLANNED_FINANCE` card, not anywhere in `nav.config.js`) — zero surface | **Lock / defer (out of the 7-item unlock scope)**: no nav item is added in Spec 038; the capability is recorded here and in `future-owner-register.md` as owned by a future backend accounting engine — computing net profit/revenue/salary totals is exactly the class of arithmetic the no-fake-money law forbids on the frontend | Permanently forbidden to fabricate on the frontend: no computed profit/loss/revenue/VAT/tax figure, ever, with or without a backend feed shown as an authored literal | No new nav item, no new page/tab; `forbidden` regex (`salary|payroll|payout|invoice|revenue|accounting|compensation|...`) on `finance.html` body stays 0-hit for this feature |
| **Analysis — Invoices & Accounts** (`management/analysis-invoices`) | `management-analysis-invoices.md:39-51,146-158` — KPI cards computing **Total Before Discount, Total After Discount, Discount, Paid, UnPaid, Overdue** (all AED totals) plus a per-family Paid/Due/Overdue breakdown table | Yes — same class of computed aggregate as analysis-expenses (sum-of-invoices math) | No nav item exists today — zero surface | **Lock / defer (out of scope)**: same disposition as analysis-expenses; recorded in `future-owner-register.md` | Same permanent boundary — computed invoice totals/discount sums are forbidden; the existing `FINANCE_SUMMARY` tiles stay row-COUNT-only by design, never a SAR sum | No nav item added; `finance.html` `FINANCE_SUMMARY` stays row-count-only (unchanged assert) |

## Summary

- **7 nav items in explicit unlock scope** (the `lockedFin` array): invoices, monthlyInvoices,
  salaries, staffSalaries, payments, classSalaryReport, banks. All 7 already have a Spec-009/030
  frontend surface (baked rows or a planned card) inside `finance.html` — Spec 038 is a nav-gate
  removal + deep-link wiring exercise, not new-content authoring, for 6 of the 7. `classSalaryReport`
  is the one open item (board-vs-lock decision deferred to `plan.md`).
- **2 legacy capabilities intentionally NOT unlocked** (analysis-expenses, analysis-invoices): both
  are computed-aggregate money pages (net profit/revenue/discount totals) with zero current
  frontend surface. They stay out of scope and are recorded as future-backend-owned in
  `future-owner-register.md` — never faked on the frontend regardless of spec number.
- **Count impact**: 0 new pages. All 7 unlocks resolve to `finance.html#view=…` deep-links; page
  total holds at 115.
